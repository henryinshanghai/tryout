是：JDK提供的一个类型；
作用：表示一个变量的值可能存在，可能不存在。
原理：
    1 当变量存在时，Optional对象只是对 源对象的简单封装；
    2 当变量不存在时，缺失的值 会被建模成为 一个 空的Optional对象 - 由于它是Optional类的一个有效对象，所以client代码在使用它时不会导致任何异常。
特征：
    为client代码提供很好的语义 - 使用Optional对象时，表示变量的值是有可能缺失的。
