package com.henry.tryout.java_8_features.refactor_test_debug_04.refactor_design_pattern_with_lambda_02.chain_of_responsibility_04;

public class Chain_of_responsibility_pattern {
    public static void main(String[] args) {
        /*
            应用场景：
                对对象进行连续的处理；
            特征：
                在完成处理A之后，需要把处理A的结果传递给下一个操作；
            SOP：
                1 准备一个 待处理对象的类型；
                2 在类型中定义一个字段 来 记录后继的对象。
                3 当前对象完成了自己的工作后，就会把结果转交给 它的后继 successor
         */
    }
}
