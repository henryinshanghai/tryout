=== 构建脚本的结构 ===
构建脚本由 “零条或多条语句” 和 “脚本块” 组成。
#1 语句可以包括“方法调用”、“属性赋值”和“局部变量定义”。
#2 脚本块是一个“以闭包作为参数”的方法调用。
    闭包被视为一个“配置闭包”，它在执行时 会配置一些“委托对象”。

下面列出了顶级脚本块。
xxx

构建脚本也是“Groovy脚本”，因此可以包含Groovy脚本中所允许的元素，比如“方法定义”和“类定义”。