// 作用：初始化 用于加载当前上下文中的bean定义 的 bean定义阅读器；
// 特征：
    #1 默认情况下是空的实现； -> 所以loadBeanDefinitions()其实是 父类中的模板方法来着
    #2 可以在子类中覆盖，比如 关闭XML的校验 或者 使用不同的XmlBeanDefinitionParser的实现；
// 参数：被当前上下文所使用的 bean定义的阅读器
protected void initBeanDefinitionReader(XmlBeanDefinitionReader beanDefinitionReader) {

}