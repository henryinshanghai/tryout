函数式编程
    特征：
        #1 开发者编写代码的基本元素是函数；
            函数是一等公民 - Java中的对象能做什么，函数式编程中的函数就能做什么
            {可以按需创建, 可以绑定到变量中, 可以作为参数传递, 可以作为另一个函数的返回值}
        #2 函数之间可以进行组合；
    原理：像是数学上的函数；
        特征：#1 没有状态； #2 没有副作用（不会改变传入的参数）；
    ---
    计算的演化（等价）：
        #1 lambda演算 -> 发展成为函数式编程语言 如Lisp；
        #2 图灵机 -> 发展成了大多数程序设计语言 如C,Java等
    ---

Java中的函数式编程 - Lambda表达式
相关概念：
    {函数式接口, 类型推演, 函数描述符, 匹配}
原理：
    编译器完成了所有需要的工作，因此开发者在编写代码时得以简化不必要的信息。
内置的函数式接口的实现：
    Predicate, Function, Supplier等

recap：
    函数作为一等功名的四个特征 - {可以随时按需创建, 可以绑定到变量, 可以作为参数传递, 可以作为返回值}

