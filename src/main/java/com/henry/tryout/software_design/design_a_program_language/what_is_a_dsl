DSL: 领域特定语言；
作用：针对于某个特定领域；
特征：
    #1 专用性强，通用性差；
    #2 最终呈现给开发者的，只是使用的语法（aka 模型对外暴露的接口）；
    #3 如果模型已经存在，则：设计一门DSL，其实就只是设计需要的接口而已
实例：
    #1 正则表达式；
        应用领域：文本处理；
    #2 配置文件；
        应用领域：定制软件行为；
        实例：Nginx的很多作用（反向代理），通过配置文件就能完成！
