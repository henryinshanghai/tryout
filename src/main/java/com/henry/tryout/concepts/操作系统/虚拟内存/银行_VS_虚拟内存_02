| 银行 | 虚拟内存 |
|------|----------|
| 银行金库里的现金 | 物理内存 |
| 所有储户 账户上的 数字总和 | 所有进程的虚拟地址 空间总和 |
| 储户 存折上的余额 | 程序看到的 虚拟地址 |
| 银行 不可能 同时被 所有储户 取光 | 操作系统 不需要 所有页面 同时在物理内存中 |
| 准备金率 | 物理内存 与 虚拟内存 的比例 |
| 存款保险/央行 兜底 | 硬盘交换空间 兜底 |

===
为什么这个比喻如此精妙

① 核心相同点：都依赖"不会同时用到所有资源"
    银行敢让5个用户各存50块（总共250块），只留100块现金，
    是因为它知道————5个人 不会 在同一秒钟 全部来取钱。

    虚拟内存 敢给 每个进程 分配4GB甚至更大的地址空间（总共可能几百GB），
    而 物理内存 只有8GB，也是因为————所有进程 不会在同一时刻 访问自己所有的页面。

这在 银行 叫"部分准备金制度"，在 虚拟内存 中叫"超分配"（Overcommit）。
本质都是用 统计规律 来 "超额承诺"。

② 都有一套"账本" 来 管理映射
    银行的账本记录：
        张三的 50块 → 对应 金库里 哪些钞票
        （或者说，张三 随时可以 取走50块，但银行 不指定 具体是哪几张钞票）
    操作系统的页表记录：
        进程A的 虚拟页3 → 对应 物理页帧17
两者都是抽象层————用户/程序 只关心 账面上的数字，不需要知道 底层具体怎么存放。

③ 都有"挤兑"风险
这是这个比喻最精彩的地方：
    银行挤兑：
        如果 5个储户 同时来取钱，银行只有100块，
        只能 先满足 前两个人，后面的人 就得排队 甚至 取不出来————银行破产
    内存抖动（Thrashing）：
        如果 所有进程 同时疯狂访问 自己不在物理内存中的页面，
        操作系统 就得 不停地 从硬盘 换入换出页面，系统变得 极其缓慢，甚至 崩溃
两者的崩溃模式 几乎一模一样————都是因为 "超额承诺" 的前提假设（不会同时全部用到）被打破了。

④ 都有"兜底机制"
    银行 挤兑时，央行可以充当 最后贷款人，紧急注入 流动性
    虚拟内存 不够时，操作系统 可以 用硬盘交换空间 来兜底，虽然慢，但至少不会 直接崩溃

===
这个比喻 还能延伸
① 银行信用 = 操作系统的信誉
    银行 能运转，靠的是 储户的信任————
        大家相信 自己的钱 随时能取出来，所以不会 去挤兑。
    虚拟内存 能运转，靠的是程序的"信任"————
        程序相信 自己声明的虚拟地址空间 是 真实可用的，所以不会去直接操作物理地址。
一旦信任崩塌（挤兑/程序 绕过虚拟内存 直接访问 物理地址），整个体系 就会崩溃。

② 银行的"贷款创造存款" ≈ 虚拟内存的"按需分配"
    银行 并不是 先有钱再放贷，而是 放贷的同时 创造了存款————
        你申请 100万房贷，银行 在你账户上 敲个数字，100万 就"凭空"出现了。
    虚拟内存 也类似————
        程序 申请10GB内存，操作系统 并不会 真的分配 10GB物理内存，
        而是 在页表中 标记 "这10GB 是 合法的虚拟地址"，
        只有 程序 真正去读写 某个页面 时，才分配 对应的物理内存（这就是"按需分页"）。

===
比喻的微小局限
唯一需要注意的是：
    银行"超额承诺"是 有意为之的商业策略（为了 放贷赚钱），
    而 虚拟内存的"超额承诺" 更多是一种 工程上的务实选择————
        因为 统计规律 告诉我们，
        绝大多数程序 在任何时刻 只用到 自己地址空间的一小部分，
        不给全部虚拟空间反而浪费。

