先说问题：没有虚拟内存会怎样？

在 没有虚拟内存的时代，程序 直接使用 物理内存地址，这带来了 几个严重问题：
① 内存碎片化
程序不断启动、退出，物理内存 被切割成 大大小小的"碎片"。
虽然 总空闲空间 可能足够，但 找不到一段 足够大的连续空间 来 加载新程序。

② 进程 互相干扰
如果 程序A 不小心（或恶意）写入了 程序B的内存区域，轻则 数据损坏，重则系统崩溃。
没有 隔离，整个系统 就像一栋 没有隔墙的办公楼。

③ 编程 极其复杂
程序员 需要操心 "我的程序 该放在 物理内存的哪个位置"、"这个地址 有没有 被别人占用"，
每个程序的运行地址 都不一样，链接 和 加载 都非常麻烦。

④ 内存容量受限
程序 必须完整加载到 物理内存中 才能运行。
如果 你的电脑 只有4GB内存，就永远无法运行 一个需要6GB内存的程序。

===
虚拟内存 怎么解决 这些问题
虚拟内存 的核心思想是：
    给 每个程序 一个"假"的、独立的、连续的地址空间，
    再 由 操作系统 负责 把 它 映射到 真实的物理内存上。

这带来了四大好处：
① 隔离保护：
    每个进程 有自己的虚拟地址空间，互不干扰，无法 越界访问 别人的数据
② 消除碎片：
    程序 看到的是 连续空间，实际 在物理内存中 可以分散存放
③ 简化编程：
    每个程序 都认为自己 独占 从地址0开始的 整块内存，程序员 无需关心 物理布局
④ 扩展容量：
    物理内存不够时，可以把 暂时不用的数据 "换出"到硬盘，需要时再"换入"
