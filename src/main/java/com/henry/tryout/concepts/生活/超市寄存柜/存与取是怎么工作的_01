超市里的寄存柜是怎么工作的？
它是怎么能 在寄存时 生成一张条形码，在取出时 又能用该条形码打开 唯一的寄存柜的呢？

超市里的寄存柜 本质上是 一套由微型计算机（单片机）控制的智能闭环系统。
它之所以能 精准地通过一张条形码 实现“一对一”的开柜，
核心在于 加密算法、随机分配机制 以及 硬件联动。

具体工作原理如下：

1. 存物时：如何生成 唯一的条形码？
当您按下“存”键时，寄存柜内部的微型计算机 会立刻执行以下操作：
① 寻找空柜 与 加密生成密码：
    系统会在内部 随机挑选一个空闲的箱格，
    并通过 独特的加密技术 自动生成一组 包含该箱号信息的唯一密码
    （通常由系统随机挑选出10位码制，重复概率极低）。
② 指令联动：
    计算机 将这串加密密码 转化为 打印指令 发送给 内置的高速热敏打印机，
    同时向 对应箱格的电子锁 发送 开锁指令。
③ 物理输出：
    打印机 将带有这串唯一密码的条形码 打印在热敏纸上 并吐出，
    对应的柜门 也会随之自动弹开。
    此时，系统将 这个条码状态 标记为“已占用”，完成 存物绑定。

2. 取物时：如何用条形码打开唯一的柜子？
取物的过程是一个“解码与验证”的过程：
① 光学扫描读取：
    当您把条码纸 对准 红光扫描口时，
    内置的条形码扫描器 会快速读取 上面的黑白条纹图案
    （只要条码图形不受损，识别率极高）。
② 数据比对验证：
    扫描器将 读取到的密码信息 传回微型计算机。
    计算机会 在内部数据库中 核对这串密码 是否有效、
    是否与 某个正在被占用的箱格 匹配。
③ 触发开箱：
    一旦校验结果一致，
    计算机就会向 该特定箱格的电控锁 发送解锁指令，柜门自动弹开。
    同时，系统会立即 将该条码作废（即一次性有效），
    防止被他人 重复使用或盗用。


3. 补充机制与安全保障
除了 常规的扫码取物，这套系统 还设计了 防呆 和 应急机制：
① 备用数字密码：
    打印出的条码纸下方 通常会 附带一串较小的数字代码。
    如果条码纸 被揉搓损坏 导致扫描器无法读取，
    顾客仍可以 通过键盘 手动输入这串数字代码 来 打开柜门。
② 管理员权限控制：
    如果遇到 条码丢失 等极端情况，
    工作人员 可以通过 高级管理权限
    （如输入 管理密码、使用 专用遥控器 或 物理应急钥匙）强行打开指定柜门，
    以 协助顾客取出物品。
