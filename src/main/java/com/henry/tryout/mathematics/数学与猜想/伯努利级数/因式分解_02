因式分解 (Factorization) 是代数中 最基础 也最重要的操作之一。
简单来说，它的定义是：
    把 一个多项式 化成 几个整式的积 的形式。
如果 把“多项式” 比作 一个复杂的数字（比如 12），
那么 “因式分解” 就是 把它拆解成 质因数相乘的形式（比如12=2×2×3 ）。

在代数里，我们 把 这种拆解后的“零件” 叫做 因式。
1. 核心概念：从“和”到“积”
① 整式乘法（展开）：是把括号打开，把“积”变成“和”。
    例如： (x+2)(x−3)=x^2−x−6
② 因式分解：是 整式乘法 的逆运算，是把 “和” 变回 “积”。
例如： x^2−x−6=(x+2)(x−3)

判断标准：
看等式的右边是不是纯乘法连接。
✅ x^2−4=(x+2)(x−2) （这是因式分解，右边是乘积）

===
总结
① 定义：和 → 积。
② 本质：整式乘法的 逆运算。
③ 流程：一提（公因式）、二套（公式）、三十字（相乘）、四分组。
④ 目的：为了 解方程、约分 和 找零点。