命名的坏习惯 & 纠正：
#1 命名过于宽泛，无法准确地描述它所代表的业务含义；
    命名应该能够准确地描述 代码的业务意图（Biz purpose）;
#2 使用技术术语来命名；
    这其实是针对实现细节的命名方式，应该建立起团队的词汇表，来避免各个开发者随意去命名；
#3 命名违反英文的语法规则；
    语法规则：类名 是一个名词；方法名 一般是动词/动宾短语；
#4 命名所使用的单词不够准确；
    比如 audit 是审计的意思，review 只是核查的意思。
    手段：团队需要建立起业务词汇表，来避免各个开发者开发时随机命名。
#5 英文单词拼写错误；
    手段：#1 IDEA的typo提示功能； #2 团队经常进行代码评审（review）；


-- extend：代码中出现重复; --
重复的分类：
    #1 代码字面意义上重复；
        手段：针对重复代码，抽取出函数；
        特征：参数是名词、函数名是动词 - 动词不相同，不代表代码就没有重复。
    #2 代码在结构上重复；
        手段：抽取出接口，用来提供一个抽象的行为；
        实践： executeTask(final Runnable runnable) {runnable.run();}
        特征：当传入的 runnable不同时，run()会执行具体的行为。

