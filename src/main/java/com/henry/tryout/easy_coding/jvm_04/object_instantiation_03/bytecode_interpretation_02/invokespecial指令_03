4 invokespecial #1 <java/lang/Object.<init> : ()V>

指令含义：
    调用 实例的初始化方法（即 构造函数）。
操作步骤：
    ① 从 操作数栈顶 弹出一个引用（作为 this参数 传给 构造函数）。
    ② 根据 常量池索引#1 找到 java/lang/Object 的<init>方法。
    ③ 执行 该构造函数的逻辑（对于 Object类，主要是 设置对象头信息，逻辑 很简单）。
    ④ 方法执行完毕 返回（()V 表示 无参数，返回 void）。
此时状态：
    栈：[ 引用地址 ] (消耗了一个，还剩下一个 dup 留下的副本)
    变量表：0: this
    对象状态：此时 堆中的对象 已经完成了 初始化，是一个 完整的可用对象。