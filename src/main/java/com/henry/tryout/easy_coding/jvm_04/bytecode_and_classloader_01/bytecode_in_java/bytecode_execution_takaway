#1 解释执行；
是：边读字节码 边翻译成 机器码；
特征：启动快，但是性能开销大。相当于 同声传译

#2 编译执行；
是：先整体读取字节码，再一次性地翻译成 机器码；
特征：启动慢，但是运行速度快；

#3 混合执行；
是：解释器 与 即时编译器(JIT)一起工作；
特征：
    ① 启动阶段，解释器立刻开始执行字节码，避免编译导致的等待；
    ② 运行阶段，JIT编译器 监控 频繁执行的代码，将之编译成为 本地机器码 并 缓存；
    ③ 后继的执行，会直接执行 缓存的机器码；