BIPUSH 13 // 把 常量13 压入 操作栈
ISTORE_1 // 并 把它保存到 局部变量表(一个数组结构)的slot_1的位置上

BIPUSH 14 // 把 常量14 压入到 操作栈中
ISTORE_2 // 把它保存到 局部变量表的slot_2的位置上

ILOAD_1 // 把 局部变量表的slot_1上的元素 加载入 操作栈
ILOAD_2 // 把 局部变量表的slot_2上的元素 加载入 操作栈
IADD // 从操作栈中 把 上方的两个数 都取出来，在CPU中 执行加法操作，并把运算的结果 压回到操作栈中
ISTORE_3 // 把 栈顶的结果 保存到 局部变量表的slot_3位置上

ILOAD_3
IRETURN // 返回 栈顶元素的值

recap：
① PUSH动作的受动者 是 操作栈；
② STORE动作的受动者 是 局部变量表； 对应 为局部变量赋值的代码[写]
③ LOAD动作的source是 局部变量表，target是 操作栈；对应 读取 局部变量的代码[读]
④ ADD动作 发生在CPU中；
⑤ IRETURN 从当前方法中返回，并 清理栈帧；对应返回语句；
