✅ 总结
load = 读入（为使用做准备）
store = 写出（为持久化或传递）
dump = 倾倒（为观察、备份或分析，强调完整性 & 原始性）

在 JVM 字节码、操作系统、数据库、调试工具等场景中，这三个词的语义始终保持这一核心区别。

如果你是在学习 JVM 字节码（比如 iload_0, astore_1），那么记住：
    load：变量 → 栈；
    store：栈 → 变量；
    dump：把整个东西打印出来看！

动作的主体 是 操作栈!