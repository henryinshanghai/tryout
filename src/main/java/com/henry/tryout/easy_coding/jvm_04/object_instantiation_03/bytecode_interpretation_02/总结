1.new 不等于 初始化：
    new 只是 分配内存，
    真正的初始化（跑 构造函数代码）是 由 invokespecial 完成的。
2.dup 是必须的：
    这是 JVM 基于栈的架构 所决定的。
    因为 函数调用 会“吃掉”参数（弹出栈），
    而我们需要 “既 初始化，又 保留引用”，所以 必须复制一份。
3.索引含义：
    Ⅰ #2 和 #1 是 常量池索引，指向 具体的类信息 和 方法信息。
    Ⅱ _1 (在 astore_1 中) 是 局部变量表索引，对应 代码中的变量 ref。