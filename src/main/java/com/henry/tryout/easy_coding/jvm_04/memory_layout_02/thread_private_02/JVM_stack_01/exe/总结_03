核心知识点总结
1.变量槽位分配：
    ① 因为是 实例方法（非static），局部变量表 索引0 被隐式占用，用于存储 this引用。
    ② 所以 x, y, z 分别占据了 索引 1, 2, 3。
    ③ 如果是 static 方法，x 就会 直接 从索引0开始。

2.运算必须上栈：
    ① JVM 不能直接写 add local_1, local_2。
    ② 必须遵循 Load (取) -> Operate (算) -> Store (存) 的模式。
    ③ 即使是 z = x + y 这样简单的代码，底层也经历了：
        取 x -> 取 y -> 加 -> 存 z。

3.返回也必须上栈：
    ① 注意第 10 行 iload_3。
        很多人会疑惑：“刚才算完不是存在 z 了吗？为什么还要 load 一次？”
    ② 因为 ireturn指令 只认 操作数栈的数据。
        无论结果是 在变量里 还是 刚算出来的，返回前 必须确保 它在栈顶。

4.优化空间：
    ① 如果开启 编译器优化（如 -O 或 JIT 编译后），
    JIT 可能会发现 z这个变量 是多余的，
    直接生成 iload_1, iload_2, iadd, ireturn，
    省略掉 istore_3 和最后的 iload_3。
    但在 标准的 javac编译结果中，为了 调试信息（Debug）的准确性，通常会 保留这些步骤。

===
Recap：
① 局部变量表 会忠实地存储 所有的局部变量；
② 运算只能发生在操作数栈中，一般流程：
    （从局部变量表）加载操作数、执行运算、写回运算结果（到局部变量表）；
③ return操作 返回的是 操作数栈的栈顶元素；