成功运行javap命令的几个影响因素：
① 源码中 是否包含有 package语句；
② 编译得到的class文件 所在的具体位置(目录)；
③ 执行java命令时，有没有 根据①+② 来 提供正确的命令格式；

总是正确的命令格式:
    javap -cp <class文件所在的根包目录> <待运行类的全限定名>

④ 目录的格式 需要 符合terminal所使用的shell中的规范，否则 目录字符串 可能会无效；