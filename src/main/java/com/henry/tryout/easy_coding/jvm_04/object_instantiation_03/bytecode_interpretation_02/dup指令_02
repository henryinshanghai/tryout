3 dup

指令含义：
    复制栈顶数值 (Duplicate)。
操作步骤：
    将 操作数栈顶的数值（刚才 new指令所产生的引用地址）复制一份，再次 压入栈顶。
为什么要这样做？
    ① 因为 下一步 调用构造函数 (invokespecial) 需要消耗掉 栈顶的引用 作为 参数（this）。
    ② 但是，我们 还需要保留 一份引用，以便 稍后赋值给 局部变量ref。
    ③ 如果 不复制，调用完 构造函数 后，栈里就空了，没法赋值。
此时状态：
    ① 栈：[ 引用地址, 引用地址 ] (栈顶 有两个 相同的地址)
    ② 变量表：0: this
