===
在你描述的条件下：
① 你在 .class文件 所在的当前目录中；
② 你的类中 包含 package 声明（例如 package com.example;）

结论：那么 你不能直接执行 java YourClass！
原因：
    因为 JVM 要求：
    必须从 “包结构的根目录” 运行，并 使用 全限定类名。

而你现在 在 “包内部”的某个子目录 里，这 违反了 Java的类加载规则。