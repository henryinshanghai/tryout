树；
    是：数据结构；
    作用：存储数据、查询数据；
    概念：
        1 节点 - 数据就存储在节点中
        2 最顶层只有一个节点(aka，根节点)；
        3 一般情况下，节点下会有多个分支 - 每个分支会引出一棵子树；
        4 最底层的节点(aka，叶子节点) 没有任何分叉；
        5 节点的高度height = 从当前节点出发，到叶子节点为止，最长简单路径上 边的数量。
        6 节点的深度depth = 从根节点出发，到当前节点为止，路径上 边的数量。
    结构特征：
        1 一个节点(aka，只有一个根节点)也是一棵树；
        2 任何一个节点与其下面的所有节点构成的树叫做 子树；
        3 根节点没有父节点，叶子节点没有子节点；
        4 除了根节点之外，任何节点都 有且仅有 一个父节点；
        5 任何节点都可以有 0~n个子节点。

二叉树：
    是：每个节点至多有两个子节点的树；
    应用：很多高效算法的基础；
    用法：平衡二叉树、二叉查找树、红黑树。