知识卡： Long值的hashCode()是怎么计算的？
注：Integer的hashCode()是 Integer值自身；
步骤：
    1 获取到 Long值的二进制数值；
    2 对 1中得到的二进制数值 右移32位；
    3 对 1中得到的二进制数值 与 2中得到的二进制数值 进行 异或处理，得到 新的二进制数值；
    4 对 3中得到的二进制数值，进行 int的 强制类型转换 - 等同于 高位被截断；
为什么使用这种算法（异或操作） 来 计算Long值的hashCode值呢？
答：避免冲突，有助于泊松分布(😳)