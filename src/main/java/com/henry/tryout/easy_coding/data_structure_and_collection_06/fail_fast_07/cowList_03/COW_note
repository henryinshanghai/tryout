启示：
    1 初始化 COW集合时，先把数据填充到 ArrayList中，然后 把 ArrayList集合作为COW的参数 - 这样能够实现批量添加；


特性：
    2 COW 适用于 读多写极少的场景；
    3 COW是 fail-safe机制 的 - 所有 并发包中的集合 都是使用 这种机制 实现的；
        原理：fail-safe 是 在一个 安全的副本 上 进行遍历操作的。
        集合的修改 与 集合的遍历 是在两个 独立的集合 上进行的。
    4 COW 没有办法 读取到 最新的数据 - 因为 拷贝得到的副本 只是一个临时的快照

用法：
