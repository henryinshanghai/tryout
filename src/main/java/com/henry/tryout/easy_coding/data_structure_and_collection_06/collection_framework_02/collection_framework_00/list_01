List
    是：一个 用来存放元素 的集合；
    特征：
        1 属于 线性数据结构, aka, 存在有 明确的上一个元素、下一个元素、第一个元素、最后一个元素；
        2 对 List对象 的遍历 结果是稳定的；
        3 最常使用的 实现类是 ArrayList；
    用法：
        1 ArrayList；
            特征：
                1 容量可变 - 可扩容/缩容；
                2 非线程安全 - 多个线程 并发操作 arrayList对象 会出现问题(覆盖、丢失等)；
                3 访问操作 很快，但是 插入与删除操作 比较慢 - 因为底层 使用的是 数组；
            存储原理：
                在内部 使用数组 来 存储元素；
                在 扩容 时，会创建 更大的数组空间，并把 旧数组中的元素 拷贝到 新的数组中；
        2 LinkedList；
            特征：
                1 链式存储(插入与删除操作 很快，但是 访问操作 比较慢) - 因为底层 使用的是 链表；
                2 继承自 AbstractList抽象类；
                3 实现了 Deque接口（double-ended queue）- 这个接口 既是栈 又是队列；
            存储原理：
                在内部 使用 双向链表 来 存储元素；
            实现原理：
                成员变量：size、first节点引用、last节点引用；