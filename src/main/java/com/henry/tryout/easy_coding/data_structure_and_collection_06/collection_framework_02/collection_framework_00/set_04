Set

特征：
    1 数据结构中 不允许存入 重复的元素 - 什么样的元素 会被视为 重复的元素?
用法：
    1 HashSet；
        特征：
            1 底层 使用 HashMap实现 - 其中key 被固定为一个 静态对象；
            2 不保证集合中 元素的存储顺序 与 元素的存入顺序 相同（remain/retain sequence's order）；
    2 TreeSet；
        特征：
            1 底层使用 TreeMap实现，是一个 树形结构；
            2 能够保证集合中 元素排列的有序性(establish the item's natural order)；
                原理：添加 新元素 到 集合中时，按照 某种规则 将之插入到 合适的位置，以此保证 有序性。
    3 LinkedHashSet；
        特征：
            1 继承自 HashSet；
            2 元素的存储顺序 与 元素的插入顺序 相同；
                原理：使用链表；