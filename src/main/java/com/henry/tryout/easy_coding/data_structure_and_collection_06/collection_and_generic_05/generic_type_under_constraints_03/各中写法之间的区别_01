===
1. List<T>：精确类型

✅ 含义
表示一个 元素类型恰好是 T 的列表。
你可以 安全地添加 T 或其子类对象（实际只能加 T 及其子类型，但编译器只允许确定是 T 的对象）。
你可以 从中读取 T 类型的对象。

✅ 示例
    List<String> list = new ArrayList<>();
    list.add("hello");      // ✅ OK
    String s = list.get(0); // ✅ OK
⚠️ 限制
① List<String> 不是 List<Object> 的子类型（泛型不协变）。
② 不能 把 List<String> 传给 参数为 List<Object> 的方法。

===
2. List<? extends T>：上界通配符（Producer）

✅ 含义
表示一个 元素类型是 T 或 T 的某个未知子类 的列表。
例如：List<? extends Number> 可以是 List<Integer>、List<Double>、List<Number> 等。
你只能从中“读取”元素，不能安全地“写入”（除了 null）。

❌ 为什么不能写入？
因为编译器 不知道 具体是哪个子类：

    List<? extends Number> list = new ArrayList<Integer>();
    // list.add(new Double(1.0)); // ❌ 编译错误！可能是 Integer 列表，不能加 Double
    // list.add(new Number());    // ❌ 也不行！
    list.add(null); // ✅ 唯一例外

✅ 你能做什么？
读取：所有元素 都可以 安全地赋值给 T 类型变量。
    Number n = list.get(0); // ✅ 安全，因为所有子类都是 Number

🎯 适用场景：只读（生产数据）
当你需要 从列表中获取 T 类型的数据，但不关心 具体子类型 时使用。

✅ 经典例子：求和
    public double sum(List<? extends Number> numbers) {
        double total = 0.0;
        for (Number n : numbers) {
            total += n.doubleValue();
        }
        return total;
    }

    // 可以传入 List<Integer>, List<Double> 等
    sum(Arrays.asList(1, 2, 3));           // OK
    sum(Arrays.asList(1.5, 2.5));          // OK

===
3. List<? super T>：下界通配符（Consumer）
✅ 含义
表示一个 元素类型是 T 或 T 的某个未知父类 的列表。
例如：List<? super Integer> 可以是 List<Integer>、List<Number>、List<Object>。
你可以安全地“写入” T 类型的对象，但读取时只能得到 Object（因为不知道具体父类是谁）。

✅ 为什么能写入？
因为无论 列表实际类型 是 Integer、Number 还是 Object，Integer(及其子类) 都能被接受。
    List<? super Integer> list = new ArrayList<Number>();
    list.add(42);        // ✅ OK，42 是 Integer，可以放进 Number 或 Object 列表
    list.add(new Integer(100)); // ✅ OK

❌ 读取受限
    Object obj = list.get(0); // ✅ 只能当作 Object
    // Integer i = list.get(0); // ❌ 编译错误！可能是 Object 列表
🎯 适用场景：只写（消费数据）
当你需要 向列表中添加 T 类型的数据，但不关心 列表 具体能存多“宽”的类型 时使用。

✅ 经典例子：Collections.copy()
    public static <T> void copy(List<? super T> dest, List<? extends T> src) {
        for (int i = 0; i < src.size(); i++) {
            dest.set(i, src.get(i)); // ✅ src 生产 T，dest 消费 T
        }
    }
src 是 ? extends T → 生产者（只读）
dest 是 ? super T → 消费者（只写）

===
🧠 总结：PECS 原则（Producer-Extends, Consumer-Super）

通配符	            |   能否读?           |	能否写？    |	角色	           |   使用场景
List<T>	            |   ✅ 读 T           |	✅ 写 T     |  精确控制          | 通用场景，需要读写
List<? extends T>	|   ✅ 读T            |	❌（除 null）| Producer（生产者） | 只读，如遍历、计算
List<? super T>	    |   ❌（只能读 Object）|	✅ 写 T     | Consumer（消费者） | 只写，如收集、填充