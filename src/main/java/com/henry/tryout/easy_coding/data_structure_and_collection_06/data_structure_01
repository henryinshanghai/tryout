数据结构：
    是：数据 逻辑意义上的组织方式 与 针对此组织方式的对数据的处理手段；
    概念：
        1 什么是逻辑意义？
            相对于物理意义而言 - 也就是数据在内存中真实的存储方式；
            逻辑意义，也就是 从物理结构上抽象出的结构。最终，仍旧是通过物理结构去实现的
        2 什么叫数据组织方式？
            也就是逻辑意义上的数据结构的分类， 比如 树、图、队列、哈希表
            树 - 二叉树、三叉树、B+树；
            图：有向图、无向图；
            队列：先进先出；
            哈希：快速定位/索引；
            特征：不同的数据组织方式，会呈现出不同的特性；
        3 什么叫 数据的处理手段？
            对数据结构本身的 CRUD + 遍历操作；
            特征：不同的数据处理方式，在性能上会存在很大差异；
    分类：
        1 线性数据结构；
            特征：结构中的每个item 有0-1个 直接前继 和 直接后继；
        2 树结构；
            特征：结构中的每个item 有0-1个 直接前继 与 0-n（n大于等于2）个直接后继；
        3 图结构；
            特征：结构中的每个item 都有0-n（n>=2）个 直接前继 与 直接后继
        4 哈希结构：
            没有 直接前继与直接后继。通过哈希函数来 关联索引与存储的值；

------
既然有这么多的数据结构 与 数据结构的处理手段，那要怎么衡量一个数据结构的性能？
手段：时间复杂度 + 空间复杂度；

算法的时间复杂度：
    作用：描述 程序执行时间 随着输入规模的增长，而增长的量级；
    用法：O(<function>);
    算法时间复杂度的优劣排序：
        O(1) > O(logn) > O(n) > O(nlogn) > O(n^2) > O(n^3) > O(2^n)

用法：什么情况下，应该使用什么样的数据结构？
    因素：操作类型、操作频率、数据量大小；
