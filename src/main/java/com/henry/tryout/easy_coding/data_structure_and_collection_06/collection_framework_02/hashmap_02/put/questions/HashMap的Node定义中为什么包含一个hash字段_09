✅ 总结：为什么 Node 需要 final int hash？

原因              |	说明
. 性能优化	     | 避免 重复计算 hashCode()，尤其 在 遍历和扩容 时
. 支持 null key	 | 统一处理 null 和 非null key，无需 特殊分支
. 快速比较	     | 先比 hash（int），再比 key（equals），减少 昂贵操作
. 架构一致性	     | 链表、树、扩容等模块 都依赖 缓存的 hash 值
🌟 这是一个典型的 “空间换时间” 优化：

用 4字节（int）的额外内存，换取 显著的 CPU时间节省。