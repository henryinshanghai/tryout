错误码是什么？有什么作用？具体是怎么用的？

错误码（Error Code） 是 软件开发 和 系统交互 中
用于 唯一标识 特定错误 或 异常状态的 数字、字符串 或 枚举值。

它是 计算机世界的 一种“通用语言”，用来 在机器之间、或者 机器与人之间，
精准地传达 “出了什么问题”，而不是 模糊地说 “出错了”。

===
1. 错误码 的 核心作用
错误码的存在 主要为了 解决 以下几个问题：

A. 精准定位问题（给 开发者 看）
① 模糊 vs 精准：
    如果 没有错误码，系统 可能只返回 “操作失败”。
    有了 错误码（如 ERR_DB_CONN_005），
    开发者 可以立刻知道 是 “数据库连接池耗尽”，而不是 SQL语法错误 或 权限问题。
② 日志检索：
    在 海量日志 中，搜索 特定的错误码 比 搜索 模糊的文本描述 要快得多，
    能迅速定位到 代码中的 具体抛出点。

B. 程序自动化处理（给机器看）
① 分支逻辑：代码 可以 根据 不同的错误码 执行 不同的补救措施。
    例如：
        遇到 429 Too Many Requests（限流），程序 可以 自动等待几秒后 重试；
        遇到 401 Unauthorized（未授权），程序 应该 直接跳转 登录页，而不是 重试。
② 解耦：
    前端或调用方 不需要 解析 复杂的错误消息文本（文本 可能会变，多语言 也会变），
    只需要判断 固定的错误码 即可。

C. 用户体验优化（给用户看）
① 友好提示：
    后端返回 错误码 PAY_BALANCE_NOT_ENOUGH，
    前端可以 将其映射为 用户能听懂的提示：“您的余额不足，请充值”，
    而不是 直接把 技术堆栈信息 甩给用户。
② 多语言支持：
    错误码是固定的，
    前端 可以 根据 用户的语言环境（中文/英文），将 同一个错误码 翻译成 不同的提示语。

D. 监控与告警
① 运维系统 可以统计 特定错误码的出现频率。
    如果 ERR_SYSTEM_OVERLOAD 在短时间内 激增，监控系统 会自动触发告警。

