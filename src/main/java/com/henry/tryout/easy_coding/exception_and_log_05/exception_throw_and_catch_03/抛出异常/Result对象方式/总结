总结
① 是什么：
    一个包含 code, message, data 的 通用包装类。
② 作用：
    统一 前后端 交互格式，分离 通信状态 与 业务状态，简化 前端错误处理。
③ 怎么用：
    1.定义 Result<T> 泛型类。
    2.Controller 所有接口 返回Result<T>。
    3.利用 全局异常拦截器 自动 将 异常 转为 Result.error()。
    4.前端 通过 拦截器 统一解析 code 和 data。

这是 现代企业级开发（尤其是 前后端分离架构）中的标准配置。