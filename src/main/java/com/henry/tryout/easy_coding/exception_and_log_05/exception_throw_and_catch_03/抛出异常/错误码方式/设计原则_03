设计 优秀错误码 的 4个原则
1.唯一性：
    整个系统（或微服务集群）中，一个错误码 只能代表 一种特定的错误含义。
2.可读性：最好能看出 模块 和 含义。
    ❌ 坏例子：1001, 1002 (谁知道是什么？)
    ✅ 好例子：
        USER_LOGIN_FAIL, ORD_PAY_TIMEOUT 或
        100101 (10 代表 用户，01 代表 登录，01 代表 失败)。
3.稳定性：
    一旦发布，错误码的含义 永远不要变。
    如果 需要新增 错误类型，申请 新码，
    不要 复用旧码 改含义，否则会导致 旧版本客户端 逻辑错乱。
4.分层处理：
    ① HTTP状态码 处理 网络层 和 协议层错误（404, 500）。
    ② 业务错误码 处理 具体的业务逻辑错误（余额不足、参数校验失败）。
    ③ 注意：
        很多现代 RESTful API 倾向于 HTTP状态码 和 业务错误码 结合使用，
        但也有一种流派 是 “所有请求 HTTP 都是 200，只靠 body 里的 code 区分”，
        这在 大型互联网企业 中 很常见，便于 网关 统一处理。

