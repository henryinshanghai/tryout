工作场景：当前的开发任务做到一半，这时突然有一个紧急的bug需要进行修复。
手段：先把当前工作区 & 暂存区已经做出的改动暂时保存并隐藏起来，等完成bugfix后，再恢复回来继续开发；
命令：git stash -u // -u用来包括没有被追踪的新文件 git stash pop
原理：
    #1 保存并隐藏时，Git会将本地变更 保存为以此新的提交，
    #2 恢复变更时，Git会取出#1中提交的文件快照，以恢复到工作区 & 暂存区。