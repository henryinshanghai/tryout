Git中的区域：
本地 - {工作区、暂存区、提交历史}
远程 - {文件目录、提交历史}

疑问：文件的id 与 提交的id有什么区别？

对象：
获取对象的方式：
    #1 使用对象的ID；
    #2 使用 表示对象的引用/变量 - 比如HEAD、branch名称(都是当前分支最后一个commit的引用)。

查看git中对象的类型 -
    手段(cat-file命令 + -t选项)：git cat-file -t <object_id>
    分类：
        #1 commit对象；- 执行commit生成的对象，包含 {提交元信息, 项目所有文件的瞬时快照}
        #2 tree对象; - 目录
        #3 blob对象； - 文件 （根据文件内容生成，与文件名没有关系）