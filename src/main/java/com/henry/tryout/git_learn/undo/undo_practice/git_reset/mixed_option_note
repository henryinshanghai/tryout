--mixed选项
    作用: 将 HEAD 重置为指定的提交 & 更新暂存区；
    特征：
        #1 mixed是 git reset命令的默认选项 - 不写选项的话，默认使用它；
        #2 mixed选项执行后，会更新暂存区 - 清除暂存区所记录的文件变更；
        #3 mxied选项不会清除 工作区所记录的文件变更；
    验证手段:
        #1 找到倒数的两个commits；- 这两个commit之间，新增了一个文件。并且这个文件在#2中还要被修改
        #2 在工作区、暂存区中做一些变更(针对 soft_option_note、mixed_option_note);
        #3 执行 git reset --mixed 命令，预期如下：
            #1 HEAD文件内容不变，master文件内容发生变化；
            #2 #2中暂存区中所记录的变更被清除；- YES
            #3 #2中工作区所进行的变更仍旧保留；- YES
        注：倒数第二个commit相对于倒数第一个commit的变更，会直接回到工作区中。
