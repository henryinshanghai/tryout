把master分支上的最新commits,合并到自己的开发分支feature上来

初始的提交&分支：
    D —— E(feature)
  /
A —— B —— C(master)

手段1：merge；
手段2：rebase；

执行后的commit图：
    D —— E —— F(feature)
  /         /
A —— B —— C(master) master可以快速地包含 commit F