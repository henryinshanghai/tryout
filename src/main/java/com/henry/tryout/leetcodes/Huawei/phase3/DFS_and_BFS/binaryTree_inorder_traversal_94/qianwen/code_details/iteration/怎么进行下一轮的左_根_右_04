已知：上一轮循环结束时，‘当前节点指针’指向右子节点；

如果 该右子节点为nil节点，则：
    while(){}会直接跳过，跟着会：
        ① 弹出栈顶节点；
        ② 移动 ‘当前节点指针’指向该节点；// 当前轮的根
        🐖 上一轮整体 作为 当前轮的左


如果 该右子节点不为nil节点，则：
    while(){}会执行：
        开始 新一轮的‘左-根-右’；
