❌ 常见错误与易错点

错误	                        说明
忘记 size = queue.size()	    导致 分钟数 计算错误（把 多层 合并成 一层）
未统计 freshCount	        无法判断 是否 全部腐烂
未特判 freshCount == 0	    可能返回 1 而不是 0
方向数组写错	                漏掉 某个方向
未修改 grid[nx][ny] = 2	    同一橘子 多次入队 → 死循环 或 超时