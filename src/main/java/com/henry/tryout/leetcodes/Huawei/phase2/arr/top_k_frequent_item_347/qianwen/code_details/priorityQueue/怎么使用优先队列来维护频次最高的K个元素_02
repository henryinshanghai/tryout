手段：使用最小优先队列；

步骤：
① 创建一个大小为k的最小优先队列，并 指定队列元素的优先级规则为 数字的频次；// 这样能够方便地查看与剔除 队列中的最小元素；
② 逐一读取 各个数字：
    Ⅰ 当 队列 未满员时，直接把数字添加到队列中；
    Ⅱ 当 队列 满员时：
        比较 当前数字的频次 与 队列中的最小频次：
            如果 当前数字的频次更大，说明 发现了更大频次的数字，则：把当前数字添加到优先队列中（自动重排元素）；
            对于其他情况，丢弃当前数字。继续读取下一个数字