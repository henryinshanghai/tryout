所谓‘有效’：① 不重复访问节点/尝试密码；② 不属于‘死亡密码’；

手段：① 记录下所有‘已尝试的密码’；② 创建‘死亡密码’的集合；

具体做法：
① 创建一个Set集合，每次尝试密码组合，就把 该密码组合 添加到 此集合中；
② 把 死亡密码的数组，先转化成列表，再转化为集合（方便快速判断 是否包含）；
③ 在遍历 所有邻居节点时，只有满足 如下条件 时，才会入队；
    1.不存在于 ‘已尝试的密码’集合；
    2.不存在于 ‘死亡密码’集合；