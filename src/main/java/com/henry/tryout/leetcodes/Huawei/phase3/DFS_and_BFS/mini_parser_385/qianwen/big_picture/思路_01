🔑 核心解题思路：递归 + 栈 / 递归下降
字符串只有三种情况：
1.纯整数：如 "123"、"-456"
2.空列表："[]"
3.非空列表："[...]"，其中 ... 是 逗号分隔的 整数 或 子列表

关键观察：
① 遇到 '[' → 开始 一个新列表
② 遇到 ']' → 结束 当前列表
③ 遇到 ',' → 分隔元素
④ 数字 可能 带负号，且 多位

✅ 这是一个典型的 “上下文无关文法”（CFG），适合用 “递归下降解析器” 实现。