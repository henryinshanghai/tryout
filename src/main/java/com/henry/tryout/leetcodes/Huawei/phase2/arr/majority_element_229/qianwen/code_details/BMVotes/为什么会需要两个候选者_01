💡 算法设计思想总结

场景	        抵消规则	        候选者数量
超过n/2	    两个不同元素抵消	1
超过n/3	    三个不同元素抵消	2
超过n/k	    k 个不同元素抵消	k-1

Boyer-Moore 投票法的本质是：
 “用 最小的候选集，覆盖 所有可能的 高频元素”


✅ 结论
需要 两个候选者，是因为 数学上 最多存在 两个“出现次数 > ⌊n/3⌋”的元素。
维护 两个候选者，配合 “三元抵消”策略，才能保证：
    所有真正的众数 II 都不会 在投票阶段 被完全淘汰。