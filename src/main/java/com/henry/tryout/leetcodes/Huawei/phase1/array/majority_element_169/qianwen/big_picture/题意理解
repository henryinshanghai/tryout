LeetCode 第 169 题 是经典题目：
多数元素（Majority Element）

📌 题目描述：
给定一个大小为 n 的数组 nums，返回其中的 多数元素。
多数元素是指：出现次数 超过 ⌊n/2⌋ 次的元素。

你可以假设：
① 数组非空
② 多数元素一定存在

✅ 示例：
输入：nums = [3, 2, 3]
输出：3

输入：nums = [2, 2, 1, 1, 1, 2, 2]
输出：2

注意：在第二个例子中，n = 7，⌊7/2⌋ = 3，而 2 出现了 4 次 > 3 → 是多数元素。