原理：for循环中的各个调用 在递归树中 处于同一个树层；
所以 如果
    ① 上一个邻居树枝的当前层中 使用了第一个2，
    ② 当前树枝的当前层中 再使用第二个2，
就会 出现 树层重复。

树层重复的条件：
① 当前元素 以及 它的前一个元素 都是2 &&；// 重复元素
② 当前树枝的同一层中，使用了 第二个2（第一个2没有被使用）；

总结：
如果 当前树枝中的第一个2没有被使用，说明 前一个树枝中已经 使用了 前一个元素，
则：
    如果再在 当前树枝中使用第二个2，就会 产生重复子集，因此 我们需要跳过第二个2.