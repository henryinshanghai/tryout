对于 遍历的当前数字，有如下3种情形：
① 与 某个‘假定众数’相等；// 说明 需要加票了
② 与 两个‘假定众数’都不相等，且 当前票数为0；// 说明需要 替换‘假定众数’了
③ 与 两个‘假定众数’都不想等，且 当前票数不为0；// 说明 需要 减票了

BM投票算法 对以上3种情形的处理顺序：
① 先处理 需要加票的情形；
② 再处理 需要替换‘假定众数’的情形；
③ 最后处理 需要减票的情形。

具体手段：
互斥的if/else if/else语法；