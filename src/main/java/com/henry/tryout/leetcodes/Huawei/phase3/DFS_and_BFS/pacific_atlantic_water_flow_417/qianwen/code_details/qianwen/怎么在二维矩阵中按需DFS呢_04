作用：得到一个 标记矩阵；

步骤：
① 对入参进行检查，如果检查没通过，则：直接return到上一级调用；
    检查维度：
        1.是否越界；
        2.是否重复访问；
        3.是否不满足题目约束（倒流：当前方格高度 >= 前一个方格的高度）；
② （如果当前方格检查通过）标记当前方格；
③ 搜索当前方格的所有邻居方格，继续递归地 对‘满足条件的岛屿方格’进行标记；