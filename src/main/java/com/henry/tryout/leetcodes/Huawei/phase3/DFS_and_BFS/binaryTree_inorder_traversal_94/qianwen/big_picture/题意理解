LeetCode 94 题是 二叉树的中序遍历 (Binary Tree Inorder Traversal)。
题目要求：
给定一个 二叉树的根节点root，返回 它的中序遍历结果（左 -> 根 -> 右）。

例如：
    1
     \
      2
     /
    3
中序遍历结果：[1, 3, 2]

① 访问 1 的左子树 (空)
② 访问 1
③ 访问 1 的右子树 (节点2)
    Ⅰ 访问 2 的左子树 (节点3) -> 访问 3
    Ⅱ 访问 2
结果：[1, 3, 2]。