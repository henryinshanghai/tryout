✅ 解题所需的基础概念
要理解这道题的解法，你需要掌握以下基本概念：

1.字符串操作：
    ① 字符串索引（如 s[i]）
    ② 子串提取（如 Python 的 s[:i]）
    ③ 字符串长度（len(s)）

2.循环与条件判断：
    ① 如何遍历 多个字符串
    ② 如何 逐字符比较

3.前缀（Prefix）的概念：
    ① 前缀 是指 从字符串开头开始的 一段连续字符。
    ② 公共前缀 就是 所有字符串 都以它开头。

4.边界处理：
    ① 空数组、空字符串的处理
    ② 某个字符串 比 其他 短的情况