✅ 总结：为什么 101 题能用迭代法？
    因为 它的递归过程 本质上是 在逐对处理 对称位置的节点，
    而这种 “成对处理”的逻辑 完全可以 用队列/栈显式模拟。

核心等价关系：
① 递归调用栈 ⇨ 手动维护的队列/栈；
② 函数参数 (left, right) ⇨ 队列中 相邻的两个元素；
③ 递归返回值组合 ⇨ 迭代中的 return false 提前终止

🔑 记住：
“能递归的问题，必能迭代” ———— 只是看你 能否找到 状态的显式表示方式。