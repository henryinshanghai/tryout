为什么需要prevHeight参数？
在 判断‘能够逆流到当前方格’时，我们 需要比较
当前方格的高度 与 其前一个方格的高度；

因此需要通过参数 来 传入 ‘前一个方格的高度’；

===
怎么初始化 prevHeight参数呢？
目标：该 初始化参数 应该能够 确保‘当前方格’会被DFS；
手段：初始化为-1；
原因：heights[][]中的元素都>=0，初始化为-1 可以保证 逆流成功。