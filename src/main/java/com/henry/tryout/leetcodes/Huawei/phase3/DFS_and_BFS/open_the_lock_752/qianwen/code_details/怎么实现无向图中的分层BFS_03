手段：使用一个队列 来 动态存储 图中每一层的节点，并 逐层处理节点。

原理：
① 队列的FIFO（先进先出）；
② 正确地使用队列：在处理队首元素时，按需向队尾添加元素；

具体做法：
① 准备一个简单队列，并把起始节点入队；
② 准备一个while循环，在循环中：
    1.获取到 队列中 当前层的节点数量；
    2.遍历 当前层的所有节点；
    3.处理 当前节点的所有邻居节点，并 按需（题目需求）入队；
③ 每次处理‘某一层’节点时，层数+1；