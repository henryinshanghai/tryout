✅ 总结：中序+全局变量的 易错点清单

易错点	                    风险	                推荐修复
全局变量未重置	            多次调用结果错误	    改用局部引用（如 Integer[]）
用 < 代替 <=	                误判 含重复值的树	    严格使用 <=
漏写递归 return	            只检查 部分路径	    确保 return dfs(left) && dfs(right)
prev 初始值设为 MIN_VALUE	无法处理最小值节点	    用 null 或 Long 类型