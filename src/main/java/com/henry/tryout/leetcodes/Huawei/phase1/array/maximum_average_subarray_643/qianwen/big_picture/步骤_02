🧠 解题思路（最优解：滑动窗口）
❌ 暴力法（O(nk)）：
对每个起始位置 i，计算 nums[i..i+k-1] 的和 → 超时！


✅ 滑动窗口法（O(n)）：
1.先计算 第一个窗口 [0, k-1] 的和 windowSum
2.然后窗口 向右滑动：
    ① 减去 左边 离开的元素 nums[i - k]
    ② 加上 右边 新进入的元素 nums[i]
3.记录 过程中最大的 windowSum
4.最后返回 maxSum / k

✅ 时间复杂度：O(n)
✅ 空间复杂度：O(1)