手段：逐一检查它是否具有IPv4地址的特征；

步骤：
① 把 字符串 使用'.'字符 分割为 多个子片段（保留所有空串片段）
② 校验 片段数量的特征；// IPv4应该有4个片段
③ 对于每一个片段：
    Ⅰ 校验 片段的长度特征；// 片段的字符数量 1~3
    Ⅱ 校验 片段的字符特征；// 片段的字符应该都是数字字符
    Ⅲ 校验 片段是否存在前导0；// 片段不允许存在前导0
    Ⅳ 校验 片段的值是否在合法范围；// 片段的值 在[0,255]之间