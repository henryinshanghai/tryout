🔑 解题核心思想

这是一个 “顺序子串匹配”问题，但 匹配单位 是 整个子数组，而不是 单个字符。

关键约束：
① 必须 按 groups 的顺序匹配
② 每个 group 必须 在 nums 中 作为 连续子数组 出现
③ 各 group 在 nums 中的位置 必须 严格递增 且 不重叠

因此，解法是：
    贪心地 从左到右扫描 nums，依次尝试 匹配 groups[0], groups[1], ...

