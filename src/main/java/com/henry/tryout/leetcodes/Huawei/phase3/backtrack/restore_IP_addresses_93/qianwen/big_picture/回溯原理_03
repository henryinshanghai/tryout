🧠 回溯的核心思想（状态空间树）
想象你在一棵树上搜索：
① 根节点：start=0, path=[]
② 每层：代表 选择 第 1/2/3/4 段
③ 每个分支：代表 当前段 取 1 位、2 位或 3 位
④ 叶子节点：path.size() == 4
    1.若 start == s.length() → 有效解
    2.否则 → 无效，丢弃

💡 “做选择 → 递归 → 撤销选择” 是 回溯的标准三步，保证探索 所有可能性 而 不互相干扰。