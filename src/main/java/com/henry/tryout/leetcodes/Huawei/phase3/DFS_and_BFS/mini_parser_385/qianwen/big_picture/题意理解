LeetCode 第 385 题：Mini Parser（迷你语法解析器） 是一道考察 递归下降解析（Recursive Descent Parsing） 和 嵌套结构处理 的经典题目。它要求你将一个表示嵌套整数列表的字符串，解析成一个由 NestedInteger 对象构成的树形结构。

📌 题目要求
给定一个 字符串s，表示一个 嵌套整数列表，例如：
① "324" → 一个整数
② "[123,[456,[789]]]" → 嵌套列表

你需要实现一个函数：
    public NestedInteger deserialize(String s)
返回对应的 NestedInteger 对象。

✅ NestedInteger 接口说明（题目提供）：
public interface NestedInteger {
    // 如果是整数，返回 true；如果是列表，返回 false
    boolean isInteger();

    // 返回整数值（仅当 isInteger() 为 true 时调用）
    Integer getInteger();

    // 返回列表（仅当 isInteger() 为 false 时调用）
    List<NestedInteger> getList();

    // 设置整数值
    void setInteger(int value);

    // 添加元素到列表
    void add(NestedInteger ni);
}
💡 你的任务 不是 实现这个接口，而是 使用它 来 构建 解析结果。