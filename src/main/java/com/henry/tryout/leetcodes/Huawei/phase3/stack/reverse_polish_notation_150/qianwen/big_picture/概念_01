🔑 要理解本题，你需要掌握以下核心概念：

1. 栈（Stack）数据结构
① 后进先出（LIFO）
② 支持 push（入栈）、pop（出栈）、peek（查看栈顶）
③ 用于暂存 中间结果

2. 逆波兰表达式的计算规则
① 从左到右 遍历每个 token：
    1.如果是 数字 → 压入栈
    2.如果是 操作符 → 弹出两个数（注意顺序！），计算后将结果压回栈
② 最终栈中 只剩一个数 → 即为 结果

3. 操作数顺序（易错点！）
① 对于 减法 和 除法，顺序 很重要！
② 栈顶 是 第二个操作数，次栈顶 是 第一个操作数
    tokens = ["a", "b", "-"] → 计算 a - b
    栈中：先 push a，再 push b
    遇到 "-"：pop() → b，pop() → a → 计算 a - b

4. 整数除法的 向零取整
① Java 中 /操作 对正数是 floor，但 对负数 也是向零（符合题目要求）
② 但要注意：不能用 Math.floorDiv（它是 向下取整）
③ 直接用 a / b 即可（题目保证 除数 不为 0）