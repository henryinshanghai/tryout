手段：按照LPS[]数组的指导，回退模式指针，直到正确的位置；

具体步骤：
① 发生失配时，根据lps[]的指导，回退 模式指针；
    patCursor = lps[patCursor - 1];
② 回退之后，继续 查看 模式字符 与 文本字符 是否匹配：
    Ⅰ 如果 失配，则：继续回退 模式指针；
    Ⅱ 如果 匹配，则：终止循环，进入 字符匹配的代码逻辑。

🐖 回退模式指针时，需要注意 别把指针回退成负数了（模式指针的最小位置为0）。