手段：出队当前方格，对于该方格的所有’有效邻居方格‘，标记并入队该邻居方格；

具体做法：
① 出队 当前状态，获取到 状态中的方格坐标 与 路径长度；
② 对于当前方格 所有8个方向上的邻居方格：
    1.检查该邻居方格是否’有效‘；// 边界检查、重复访问检查、业务约束检查；
    2.如果有效，则：
        Ⅰ 查看是否是终点方格，如果是，则：计算出最短路径长度，直接返回；
        Ⅱ 标记该邻居方格为’已访问‘，并 把它入队。
③ 如果循环结束时，方法仍旧没有返回，说明 BFS没有搜索到目标路径，则：返回-1.