LeetCode 第 1 题 是经典入门题：
两数之和（Two Sum）

📌 题目描述：
给定一个整数数组 nums 和一个目标值 target，
请你在该数组中找出 两个整数，它们的下标不同，且和等于 target。
返回这两个整数的下标（顺序任意）。

你可以假设：
① 每组输入 恰好 有一个解
② 不能 重复使用 同一个元素（即不能用 nums[i] + nums[i]）

✅ 示例：
输入：nums = [2, 7, 11, 15], target = 9
输出：[0, 1]
解释：nums[0] + nums[1] = 2 + 7 = 9