算法步骤：
1.初始化 一个空栈（可用 Deque<Character>）
2.遍历 每个字符 c：
    ① 如果 c 是左括号（(, {, [）→ 压入栈
    ② 如果 c 是右括号（), }, ]）：
        Ⅰ 若 栈为空 → 无效（没有左括号匹配）
        Ⅱ 若 栈顶 不是 对应的左括号 → 无效
        Ⅲ 否则 → 弹出栈顶（成功匹配一对）
3.遍历结束后，栈 必须为空（所有左括号都被匹配）

✅ 时间复杂度：O(n)
✅ 空间复杂度：O(n)（最坏情况全是左括号）