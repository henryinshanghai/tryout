🧩 组合逻辑：AND 关系
当前两棵树镜像 ⇨ 外侧对称 AND 内侧对称

    return (left.val == right.val)
        && isMirror(left.left, right.right)   // 子问题1的解
        && isMirror(left.right, right.left);  // 子问题2的解
✅ 子问题的布尔结果 直接参与 当前层的最终判断。