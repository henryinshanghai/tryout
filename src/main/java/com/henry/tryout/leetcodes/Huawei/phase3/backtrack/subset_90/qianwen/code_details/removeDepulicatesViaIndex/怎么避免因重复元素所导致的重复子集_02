当发现’树层重复‘时，跳过 当前元素（继续使用下一个元素构造子集）

树枝重复 VS. 树层重复

[1, 2, 2]

在构造1字头，3个元素的子集([1, 2, 2])时，
第一个2选择后，仍旧能够选择第二个2；// 树枝上的元素重复

特征：树枝上的元素重复是合法的；
原因：因为 树枝上的重复 本质上 是在做追加；

==
构造1字头，2个元素的子集时，
第一个2被选择后，就不能再选择第二个2了。// 树层上的元素重复

特征：树层上的元素重复 是非法的；
原因：因为 树层上的重复 本质上 是在做替换，替换为相同的元素 会得到 重复的子集；