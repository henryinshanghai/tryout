手段：list.toArray(<prepared_arr>)；

代码：
return mergedRangeList.toArray(new int[mergedRangeList.size()][]);

解释：
✅ 总结

代码	                                作用
mergedRangeList	                    List<int[]>，存储 合并后的区间
new int[mergedRangeList.size()][]	创建一个 长度为N的 int[][] 空数组（N=区间数）
.toArray(...)	                    将 列表内容 复制到 该数组，并返回 int[][]

💡 记住口诀：
“List 转数组，模板 要传好；
类型 加 长度，安全又高效。”

这行代码是 Java 中 将 泛型列表 转为 具体类型数组 的标准做法，务必掌握！