✅ 解题思路：BFS（广度优先搜索）
🧠 核心思想
1.从 起点 (0, 0) 开始，将 其 加入队列，路径长度 = 1
2.每次 从队列取出 一个位置，尝试向 8 个方向 移动
3.如果 新位置 合法（在网格内、是 0、未访问过），则：
    ① 标记为已访问
    ② 加入队列
    ③ 路径长度 = 当前长度 + 1
4.一旦到达 (n-1, n-1)，立即返回 当前路径长度
5.队列空 仍 未到达 → 返回 -1

📌 关键细节
① 8方向移动（不是 4 方向！）：
    (-1,-1) (-1,0) (-1,1)
    ( 0,-1)  起点   ( 0,1)
    ( 1,-1) ( 1,0) ( 1,1)
起点或终点 是 障碍 → 直接返回 -1