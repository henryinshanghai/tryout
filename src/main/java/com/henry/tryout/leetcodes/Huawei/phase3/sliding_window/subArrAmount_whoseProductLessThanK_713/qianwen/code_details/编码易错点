❌ 常见错误与易错点

错误	                    说明
忘记 k <= 1 特判	        输入 k=0 或 k=1 时返回错误结果
用 int product 导致溢出	中间乘积 超 int 范围，变成 负数 → 逻辑错误
计数方式错误	            如 count++（只计当前元素）或 count += windowSize 在错误时机
窗口收缩条件写错	        如 while (product > k)（应为 >= k）
试图用前缀积 + 二分	    因 乘积 无单调性（除法不可逆），难以实现