🔑 核心解题思路：回溯 + 剪枝（允许重复选择）
这是一个典型的 “子集型回溯” 问题，但与 普通子集 不同：每个元素 可选多次。

关键思想：
① 从 candidates 中 按顺序 选择元素（避免重复组合）
② 对当前元素，可以选 0 次、1 次、2 次……直到 和 超过 target
③ 使用 递归 探索 所有可能，并 在满足条件时 记录结果

💡 为什么 按顺序选 能 避免重复？
因为我们规定：一旦 跳过 某个元素，后续 不能 再回头 选它。
例如：先选 2，再选 3 → 允许；但先选 3，再选 2 → 不允许（因为 2 在 3 前面，已“跳过”）。