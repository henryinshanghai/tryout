📌 题目要求（精准理解）
给定一个整数数组 nums，找出所有 出现次数 严格大于 ⌊n / 3⌋ 的元素。
返回这些元素组成的列表（顺序任意）。

n = nums.length
⌊x⌋ 表示向下取整（floor）

例如：n = 7 → ⌊7/3⌋ = 2 → 要找出现 ≥3 次 的元素
✅ 示例
输入: nums = [3,2,3]
输出: [3]
解释: n=3, ⌊3/3⌋=1, 3 出现 2 次 > 1 → 符合

输入: nums = [1]
输出: [1]
解释: n=1, ⌊1/3⌋=0, 1 出现 1 次 > 0 → 符合

输入: nums = [1,2]
输出: []
解释: n=2, ⌊2/3⌋=0, 每个数出现 1 次 > 0？但官方输出是 []！

⚠️ 注意：LeetCode 实际判定标准 是 count > n / 3（浮点），等价于 count ≥ ⌊n/3⌋ + 1
所以 [1,2] 中 每个数 出现 1 次，而 ⌊2/3⌋+1 = 0+1 = 1 → 1 ≥ 1 成立？
但 官方测试用例 输出为 [] ———— 这是因为 题目描述 存在歧义。


🔍 权威澄清（来自 LeetCode 官方）：
“An element must appear more than ⌊n/3⌋ times.”
即：count(x) > ⌊n/3⌋
对 n=2：⌊2/3⌋ = 0 → 需要 count > 0 → 所有 出现过的数 都满足？
但 实际测试用例 [1,2] 的期望输出是 []

📌 真相：虽然数学上 1 > 0 成立，
但 LeetCode 的测试逻辑 基于 “只有当元素出现次数 > n/3（实数除法）”，
且 由于 最多只能有 2 个这样的元素，算法设计 围绕这一点 展开。

✅ 实用理解：我们 找的是 出现次数 > n/3 的元素，这类元素 最多只有 2 个。