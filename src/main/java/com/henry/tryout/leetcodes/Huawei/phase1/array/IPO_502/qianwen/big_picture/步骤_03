如何高效实现？

方法一：排序 + 最大堆（推荐！效率高）
1.将所有项目按 capital 升序排序。
2.用一个指针 i 遍历排序后的项目列表。
3.用一个最大堆（存 profits）维护当前所有可启动的项目。
4.每轮：
    ① 将 所有 capital[i] <= w 的项目 加入 最大堆（通过 移动指针 i）
    ② 如果堆 非空，取出 最大利润，加到 w
    ③ 否则，提前终止（没钱 启动 任何项目了）

✅ 时间复杂度：O(n log n + k log n)
✅ 空间复杂度：O(n)
⚠️ 注意：不能 先按 profit 排序，因为 capital 是 启动门槛！