手段：在中序遍历的过程中，记录下’序列中的当前节点‘，并作为’序列中的前一个节点‘ 来 与后继节点比较。

具体做法：
① 先 判断当前节点 是否 违反了 BST的数值约束；
    手段：比较 当前节点 与 ’序列中的前一个节点‘（需要最开始时提供一个虚拟的初始值）
② 再 把’当前节点‘ 记录为 ’序列中的前一个节点‘；
    手段：把 当前节点（的值） 赋值给 previousNode(Val)变量