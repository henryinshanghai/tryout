手段：列表的流式操作；

===
🔍 链式调用逐步解析
    return numSeqSortByFreqInDesc
        .stream()           // ① 转为 Stream<Integer>
        .mapToInt(i -> i)   // ② 转为 IntStream（基本类型 int 流）
        .limit(k)           // ③ 只取前 k 个元素
        .toArray();         // ④ 转为 int[] 数组返回

==
✅ .stream()
① 将 List<Integer> 转换为 Stream<Integer>
② 启用 函数式 流水线操作（如 map、filter、limit 等）
💡 此时 流中的元素是：5, 2, 8, 1, ...（已按 频次 降序排好）

==
✅ .mapToInt(i -> i)
① 把 Stream<Integer>（包装类型）转换为 IntStream（基本类型 int 流）
② i -> i 是一个 identity function（恒等函数），只是做 类型转换

为什么要转 IntStream？
因为最终要返回 int[]，而：
① Stream<Integer>.toArray() 返回的是 Object[] 或 Integer[]
② 只有 IntStream.toArray() 才能返回 int[]
 ✅ 这是 Java Stream 中处理基本类型的标准做法

==
✅ .limit(k)
① 截断流，只保留前 k 个元素
② 因为列表 已经是 频次降序，所以 前k个 就是「频次最高的 K 个数字」

🧠 关键点：
排序 + limit = Top-K

==
✅ .toArray()
① 将 IntStream 转为 int[] 数组
② 例如：若前 3 个是 [5, 2, 8]，则返回 new int[]{5, 2, 8}

===
⚠️ 注意事项

问题	                说明
是否包含重复？	    不包含！
                    因为 numSeqSortByFreqInDesc 来自 map.keySet()，是去重的
如果 k > 列表长度？	limit(k) 会 自动 取全部，不会报错
能否不用 mapToInt？	可以，但会返回 Integer[]，而题目要求 int[]


✅ 总结：每一步的作用

步骤	                作用	                为什么需要
.stream()	        启用流式处理	        为了使用函数式 API
.mapToInt(i -> i)	转为基本类型 int 流	为了最终生成 int[]
.limit(k)	        取前 K 个	        获取 Top-K 结果
.toArray()	        转为数组	            满足返回类型 int[]

💡 核心逻辑链：
已排序列表 → 转流 → 取前 K → 转数组 = 频次最高的 K 个数字