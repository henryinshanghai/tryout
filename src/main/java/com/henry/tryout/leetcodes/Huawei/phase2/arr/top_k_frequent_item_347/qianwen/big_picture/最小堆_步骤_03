方法二：哈希表 + 最小堆（优先队列）✅（最优常用）
1.用 哈希表 统计频率
2.维护一个 大小为 k 的最小堆（堆顶 是 当前第 k 高频）
3.遍历所有元素：
    ① 如果 堆未满（< k），直接加入
    ② 否则，若 当前频率 > 堆顶频率，则 弹出堆顶，加入 当前元素
4.最终 堆中 就是 前 k 高频元素

时间复杂度：O(n log k)
空间复杂度：O(n)（哈希表）+ O(k)（堆）
优点：当 k << n 时，比全排序更快；是面试标准解法