LeetCode 第 409 题 是 「最长回文串」（Longest Palindrome），题目描述如下：
给定一个 包含 大写字母 和 小写字母 的 字符串 s，返回 可以 由 这些字符 所构造出的 最长回文串的长度。
字符 区分 大小写（例如 "Aa" 不是回文）。

🌰 示例
① 输入："abccccdd"
    输出：7
    解释：可构造回文串如 "dccaccd"，长度为 7。
② 输入："a" → 输出：1
③ 输入："bb" → 输出：2

