总结
① for 循环内部：是在 消耗当前的步数，去探索 下一个步数的可能性。
② for 循环外部：标志着 当前步数阶段 彻底结束，正式跨入 新的步数阶段。

如果你把 rollSteps++ 放在 for 里面：
    处理第 1 个节点时，步数变 1。
    处理第 2 个节点时，步数变 2。
这就乱了！明明都是 从 "0000" 转一次得到的，怎么第二个 就变成了 转两次呢？

所以，必须 在 当前层所有节点 都处理完毕 后，才能 增加步数计数。