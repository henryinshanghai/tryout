优先队列：优先级高的元素 排在前面；
为了能够 移除不需要的元素，我们选择的优先级是：
    频次小的、字典序大的；
而 题目要求的输出顺序为：
    频次从高到低、字典序升序；
结论：如果 连续poll()出 队列中的单词，得到的单词列表的顺序 会与题目要求刚好相反。

手段：把poll()出的单词列表，逆序处理；
具体做法：
    Collections.reverse(resultWordList);