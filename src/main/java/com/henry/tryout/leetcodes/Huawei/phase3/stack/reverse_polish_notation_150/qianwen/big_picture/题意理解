LeetCode 第 150 题是 「逆波兰表达式求值」（Evaluate Reverse Polish Notation），是一道经典的 栈（Stack）应用题。它考察你对后缀表达式的理解以及栈的模拟操作能力。

🧩 题目描述（简化）
给定一个 字符串数组 tokens，表示一个 逆波兰表达式（后缀表达式），
计算其值。

什么是 逆波兰表达式？
特征：
    ① 操作符（+, -, *, /）写在 操作数 之后
    ② 不需要 括号，天然 无歧义
    ③ 计算时 从左到右扫描，遇到 操作符时 就 对 最近的两个操作数 进行运算

示例：
输入：tokens = ["2","1","+","3","*"]
输出：9
解释：(2 + 1) * 3 = 9

输入：tokens = ["4","13","5","/","+"]
输出：6
解释：4 + (13 / 5) = 4 + 2 = 6 （注意：整数除法 向零取整）
⚠️ 注意：除法 是 向零取整（truncate toward zero），不是 向下取整！
例如：13 / 5 = 2，-13 / 5 = -2（不是 -3）