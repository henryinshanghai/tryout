手段：使用两个指针来 分别标识单词的开头与结尾；

具体做法：
① 指向单词末尾的字符指针；backwardsWordEndCursor
② 按需停下的句子字符指针；// backwardsSentenceCharCursor

具体用法：
① 从后往前地 移动字符指针，
② 识别 单词的开头位置：
    在 当前句子字符指针 指向 一个空格字符 时，
    说明 该位置的下一个位置 就是 某个单词的开头；
③ 识别 单词的末尾位置：
    （继续移动字符指针）在 当前句子字符指针 指向一个非空字符 时，
    说明 该位置 就是 某个单词的末尾；
