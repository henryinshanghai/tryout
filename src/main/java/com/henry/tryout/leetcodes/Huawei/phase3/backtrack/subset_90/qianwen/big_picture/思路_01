🔑 核心解题思路：回溯 + 排序 + 跳过重复
这是「子集」问题（LeetCode 78）的扩展。
关键区别在于：如何避免 因 重复元素 导致的 重复子集。

核心思想：
1.先排序：让 相同元素 相邻，便于 识别 和 跳过;
2.在 回溯过程 中，对 同一层（same level）的重复元素 只选一次
    即：如果 当前元素 与 前一个元素 相同，且 前一个 在同一层 未被选择，则 跳过
💡 这称为 “同层去重”，是 处理 重复元素回溯问题 的 关键技巧。