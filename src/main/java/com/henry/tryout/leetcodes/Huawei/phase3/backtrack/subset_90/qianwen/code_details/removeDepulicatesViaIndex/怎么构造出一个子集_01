① 从 原始元素序列 中，先选定一个元素 作为 所构造子集 的第一个元素；
② 从 剩余的元素集合中，继续选择元素，并把 选择的元素 追加到 所构造的子集中。

策略：定锚指针 + 子问题；
特征：元素可选范围为 [startIndexAnchor, length - 1]，
    随着 定锚指针 逐步后移，元素可选范围会逐渐收窄；
作用：避免 因为回头选择元素 而产生重复子集。

简单示例：
{1, 2, 2}

{1}
{1, 2}
{1, 2} // 仍旧构造出了 重复子集
{1, 2, 2}
{2}
{2, 2}
{} // 空集