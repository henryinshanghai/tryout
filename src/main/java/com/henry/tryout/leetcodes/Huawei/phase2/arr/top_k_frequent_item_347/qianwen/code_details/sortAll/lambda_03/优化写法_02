这是一个非常棒的问题！我们来深入理解为什么：
    Comparator.comparingInt(x -> map.get(x)).reversed()
能实现 按频次 降序排序。

===
✅ 分两步理解
第一步：Comparator.comparingInt(x -> map.get(x)) 是什么？
这是 Java 提供的一个工厂方法，它 会生成 一个 “按 map.get(x) 升序排序” 的比较器。
① 它等价于：
    (a, b) -> Integer.compare(map.get(a), map.get(b))
② 而 Integer.compare(p, q) 的行为是：
    如果 p < q → 返回负数 → a（第一个参数） 排在 b（第二个参数） 的前面 → 升序
✅ 所以：
    comparingInt(x -> map.get(x)) = 按频次升序（低频 → 高频）

第二步：.reversed() 做了什么？
reversed() 是 Comparator 接口的一个方法，作用是：
将 当前比较器的排序顺序 完全反转。
① 原来返回 -5（a 在前） → 现在变成 +5（a 在后）
② 原来返回 +3（a 在后） → 现在变成 -3（a 在前）

✅ 所以：
    升序 + reversed() = 降序

===
🔁 整体效果

步骤	                            比较器行为	    排序结果
comparingInt(x -> map.get(x))	频次小的排前面	[低频, ..., 高频]（升序）
.reversed()	                    反转上述顺序	    [高频, ..., 低频]（降序）✅

===
✅ 总结
    Comparator.comparingInt(x -> map.get(x)).reversed()
之所以 能实现 频次降序，是因为：
① comparingInt(...) 生成一个 按频次升序 的比较器；
② .reversed() 将其 反转为 降序；
③ 最终效果 = 高频元素 排在前面 ✅

🌟 记住：
“comparing = 升序，reversed = 降序”