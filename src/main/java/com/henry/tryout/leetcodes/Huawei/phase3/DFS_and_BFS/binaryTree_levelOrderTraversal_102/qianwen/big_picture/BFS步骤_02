✅ 解题思路：BFS（广度优先搜索）

🧠 核心思想
1.从 根节点 开始，将 其 加入队列
2.每次处理 一整层：
    ① 记录 当前队列大小 size = queue.size()
    ② 循环 size 次，取出节点 并 加入 当前层结果
    ③ 将其 非空左右子节点 加入队列（为 下一层 准备）
3.重复 直到 队列为空

📌 为什么用 queue.size() 控制一层？
① BFS 中，队列 在某时刻的大小 = 当前层的节点数
② 因为 上一层的所有节点 都已入队，且 尚未处理 下一层