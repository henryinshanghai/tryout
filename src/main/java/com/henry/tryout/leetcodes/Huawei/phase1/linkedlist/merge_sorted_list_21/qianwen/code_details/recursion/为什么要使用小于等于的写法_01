leetcode21题的递归解法 为什么if中是<=？可以是<吗？

===
✅ 两种写法都正确吗？
是的！都正确。
因为题目只要求：
    返回一个 合并后的 升序链表
并没有要求：
    ① 相等元素 必须来自 哪个链表
    ② 保持 原链表的“相对顺序”（虽然 链表 本身有序，但 跨链表的 相等值 顺序 未规定）
所以 无论 先取 l1 还是 l2 的相等节点，结果 都是 合法的升序链表。


===
🎯 为什么大多数题解用 <=？
1.习惯性“左优先”
    在 归并排序、合并等 场景中，通常 优先取 左边（l1）的元素，保证 稳定性（虽然本题 不要求稳定，但 习惯如此）。
2.语义更自然
    “如果 l1 不大于 l2，就选 l1” ———— 包含相等情况，逻辑完整。
3.避免“偏向右”的误解
    如果用 <，有人 可能误以为 “只有 严格小 才选 l1”，
    而 <= 明确表达了 “小于或等于 都选 l1”。