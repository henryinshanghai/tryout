🌟 为什么 k+1 是上限？再深入一点

假设 a = "abc", b = "cabcabca"（长度 8）
① k = ceil(8/3) = 3
② a 重复 3 次："abcabcabc"（长度 9）
③ 但 b 从 a 的第 2 位 'c' 开始：c|abcabca
    需要：[abc] [abc] [abc] [abc] → 实际需要 4 次？
    但 k+1 = 4，所以 能覆盖
而如果 b 无法 在 k 或 k+1 次中出现，说明：
    ① b 包含 a中没有的字符，
    ② 或者 结构不匹配，
再多重复也没用。