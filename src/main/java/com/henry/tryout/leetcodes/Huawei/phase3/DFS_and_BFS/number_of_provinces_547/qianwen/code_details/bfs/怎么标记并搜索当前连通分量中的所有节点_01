手段：BFS；
原理：BFS能够搜索并标记 一个连通分量中的所有节点；

具体做法：
① 准备一个节点的简单队列，初始把起始节点入队；
② 标记起始节点为‘已访问’；
③ 准备一个while循环：
    出队队首节点 作为 当前节点；
    遍历所有节点：
        如果 该节点 与 当前节点 相连通，并且 还没有被标记，// “未被标记的邻居节点”
        则：
            1.标记该节点；
            2.入队该节点。