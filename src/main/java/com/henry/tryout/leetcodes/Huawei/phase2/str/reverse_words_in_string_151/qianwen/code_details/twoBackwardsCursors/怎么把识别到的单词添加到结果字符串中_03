StringBuilder 的 append(CharSequence s, int start, int end) 方法
是 Java 中一个 高效追加子序列 的工具。
下面详细解释 其作用、参数含义、使用示例 和 注意事项。

===
✅ 方法签名
    public StringBuilder append(CharSequence s, int start, int end)
📌 注意：这个方法从 Java 1.8（JDK 8）开始引入


🔍 参数详解

参数	    类型	            含义	                    要求
s	    CharSequence	要 从中截取子序列的 源对象	不能为 null
start	int	            起始索引（包含）	        0 ≤ start ≤ end
end	    int	            结束索引（不包含）	        start ≤ end ≤ s.length()

💡 关键记忆点：
[start, end) ———— 左闭右开区间，和 String.substring(start, end) 一致！

===
✅ 功能说明
该方法 将 s 中 从索引 start 到 end - 1 的字符 追加到 StringBuilder 的末尾。

等价于：
    sb.append(s.subSequence(start, end));
    // 或
    sb.append(s.toString().substring(start, end));
但 更高效！因为它避免了创建中间 String 对象。

===
✅ 总结

要点	    说明
作用	    将 CharSequence 的 [start, end) 子序列 追加到 StringBuilder
区间	    左闭右开（含 start，不含 end）✅
优势	    避免中间 String 对象，性能更高
安全	    会 检查边界，越界 抛异常
适用	    JDK 8+，处理 子串拼接的 首选方式

💡 记住口诀：
“append(CharSequence, start, end) —— 左闭右开，高效无中间。”