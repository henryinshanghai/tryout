手段：
① 先遍历 基准字符串中的 各个字符，获取到 当前位置上的字符；
② 再 遍历 字符串数组中的 其他字符串；
    获取 当前字符串；
    比较 当前字符串 的 当前位置上的字符 与 基准字符串 在当前位置上的字符：
        如果 相同，说明 字符匹配，则：
            继续比较 下一个字符串 当前位置上的字符；
        如果 不相同，说明 最长公共前缀 已经找到，则：
            从 基准字符串 中 按需截取出 最长公共前缀子串；
            直接返回，不需要 继续遍历其他字符串了。

整体来看：
① 先 固定 位置指针（确定当前字符）；
② 再 移动 字符串指针（逐个比较 当前字符串的当前字符 与 基准字符串中的当前字符）；