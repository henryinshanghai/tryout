✅ 最佳实践总结

易错环节	    正确做法
变量声明	    用 Integer cand1 = null
条件顺序	    先匹配候选 → 再占空位 → 最后抵消
验证必要性	必须第二遍 真实计数
验证写法	    两个独立 if + 判 null
计数重置	    验证前 count1 = count2 = 0
结果收集	    分别判断 count1 > n/3 和 count2 > n/3