✅ 检查项	    📌 说明	                            ✅ 正确做法
1. 空输入处理	是否考虑了 s 或 t 为空的情况？	        显式判断：if (s.isEmpty()) return true;（空串是任何串的子序列）
2. 循环边界保护	循环条件 是否防止 两个指针 越界？	    使用 双重条件：while (i < s.length() && j < t.length())
3. 安全访问元素	访问 s.charAt(i) 或 t.charAt(j)      所有字符访问 必须 在循环条件 或 显式检查 下 进行，避免越界
                前 是否确保 索引有效？
4. 指针移动逻辑	哪个指针该动？何时动？是否符合题意？	    子序列指针 i 仅在 匹配成功 时 前进；主串指针 j 始终前进（不可回退）
5. 提前返回安全性	若使用 提前返回，是否覆盖 所有边界？	    更推荐 循环结束后 统一判断：return i == s.length();
                                                    避免 提前返回的复杂性
6. 自增操作顺序	是先判断 还是先自增？语义是否清晰？	    匹配后立即 i++，再判断 是否完成：
                                                    if (++i == s.length())（若用提前返回）或 直接 i++ 后 循环结束判断
7. 最终结果判断	是否 正确判断 “是否完全匹配”？	        返回 i == s.length()，因为 i 表示 已成功匹配的字符数量