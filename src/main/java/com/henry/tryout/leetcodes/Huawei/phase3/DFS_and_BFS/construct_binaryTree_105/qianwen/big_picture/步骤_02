🧠 解题思路详解
✅ 步骤 1：建立 中序值 到 索引的映射
    Map<Integer, Integer> inMap = new HashMap<>();
    for (int i = 0; i < inorder.length; i++) {
        inMap.put(inorder[i], i);
    }

✅ 步骤 2：设计递归函数
我们需要知道：
① 当前子树 在 preorder 中的 起始位置（因为 前序 是“根-左-右”，连续存储）
② 当前子树 在 inorder 中的 左右边界 [inStart, inEnd]

但注意：不能直接用 preStart 和 preEnd，因为 左右子树长度 由中序决定。
更优策略：
① 用一个 全局指针 preIndex（或引用传递）记录 当前用到的是 preorder的哪个位置
② 因为 前序 是“根→左→右”，我们 按顺序消费 preorder 即可