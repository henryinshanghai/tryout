按照 发生字符匹配才把 尝试字符指针向后移动一个位置的 规则，
如果 在文本字符序列 被遍历完成之前，尝试字符指针 被移动到了 尝试字符序列的末尾，则：
证明 该尝试字符序列 是 文本字符序列的 一个“子序列”。

if(cursorOfTriedSeq == triedCharSeq.length()) return true;

===
手段1：
① 先按照规则，无脑移动 两个指针；
② 在循环结束后，查看 尝试字符指针的位置；

===
手段2：// 更好的做法
① 在 遍历过程 中，每次移动 尝试字符指针（字符匹配）时，就 即时检查 字符指针的位置；
如果 到达 尝试字符序列的末尾，说明 它是一个子序列，返回true；
② 在 循环结束 后，返回false，表示 不是子序列。
