总结

在你的代码中，sentinelInstance 被设为 static 成员变量，主要是为了：
① 利用 == 进行 快速且绝对可靠的 身份识别（区分 标记 和 数据）。
② 节省 内存 和 GC开销（全局复用，只创建一次）。
③ 语义清晰，表明 它是一个 全局通用的控制标记，而非 业务数据。

如果 不设为 static，而是在方法内 new，
代码功能 依然正确，
但 会失去 上述的性能优势 和 “全局唯一身份”的 语义强调。