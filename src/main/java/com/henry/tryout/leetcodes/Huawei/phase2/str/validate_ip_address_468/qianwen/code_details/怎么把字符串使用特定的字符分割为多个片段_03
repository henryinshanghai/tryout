String.split(String regex, int limit) 是 Java 中
用于 按正则表达式 分割字符串的核心方法。

它是 split(regex) 的完整版本，通过 limit参数 来 控制分割行为。
下面详细解析 其作用 和 参数含义。

✅ 方法签名
    public String[] split(String regex, int limit)
📌 这是 String 类的实例方法，返回一个 String[] 数组。


🔍 参数详解
1. regex（正则表达式）
① 作用：指定 分隔符的模式
② 类型：String（但 会被编译为 正则表达式）
③ 注意：
    Ⅰ 如果 要匹配 字面量字符（如 .、|、*），需转义！
    Ⅱ 例如：split("\\.") 分割 点号，split("\\|") 分割 竖线

🧪 示例：
    "192.168.1.1".split("\\.");
    // 结果: ["192", "168", "1", "1"]

    "a|b|c".split("\\|");
    // 结果: ["a", "b", "c"]

2. limit（限制分割次数）
这是 最关键 也最容易误解的 参数！它有三种情况：

limit值	行为	                                说明
n > 0	最多分割 n-1 次，结果数组长度 ≤ n	    保留 尾部空串
n = 0	尽可能分割，丢弃 尾部空串	            等价于 split(regex)
n < 0	尽可能分割，保留 所有空串（包括尾部）	最完整分割
💡 核心规则：
limit 控制“是否保留 尾部空字符串” 以及 “最多分几段”；

===
🧪 详细示例（以 "a:b::c" 为例）
情况 1：limit > 0（保留尾部空串，最多 n 段）
    String s = "a:b::c";

    s.split(":", 2);  // 最多分 2 段 → ["a", "b::c"]
    s.split(":", 3);  // 最多分 3 段 → ["a", "b", ":c"]
    s.split(":", 5);  // 最多分 5 段 → ["a", "b", "", "c"] （实际只有4段）
    s.split(":", 6);  // 最多分 6 段 → ["a", "b", "", "c"] （不会补空串到6个）
✅ 特点：
① 实际分割次数 = min(最大可能分割数, limit - 1)
② 不会 在末尾添加 额外空串（只保留 自然产生的）


情况 2：limit = 0（默认行为，丢弃尾部空串）
    String s = "a:b:c::";

    s.split(":");     // 等价于 split(":", 0)
    // 结果: ["a", "b", "c"] → **尾部两个空串被丢弃！**

    s.split(":", 0);  // 显式写法
    // 结果: ["a", "b", "c"]
⚠️ 这是最常用 但也最容易踩坑的行为！


情况 3：limit < 0（保留所有空串）
    String s = "a:b:c::";

    s.split(":", -1);
    // 结果: ["a", "b", "c", "", ""] → **尾部空串 全部保留！**

    s.split(":", -2); // 任何负数效果相同
// 结果: ["a", "b", "c", "", ""]
✅ 用途：当你需要 精确知道 字段数量时（如 CSV 解析）

===
✅ 总结：limit 参数速查表

limit	分割次数	        尾部空串	典型用途
> 0	    最多 limit-1 次	保留	    取前 N 段
= 0	    尽可能多	        丢弃	    默认行为
< 0	    尽可能多	        保留	    精确字段数（如 CSV）

💡 记住口诀：
“正数 限段数，零 丢尾空串，负数 全保留。”