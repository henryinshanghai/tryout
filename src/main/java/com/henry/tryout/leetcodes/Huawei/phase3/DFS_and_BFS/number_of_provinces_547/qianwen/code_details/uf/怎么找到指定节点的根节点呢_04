森林中树的结构特征：
① 每个节点 都有自己的父节点；
② 树的根节点的父节点 就是它自己；// 相当于存在有一个指向自己的环

手段：一直查询当前节点的父节点，直到某个父节点 满足 “它的父节点就是它本身”；

具体做法：
① 判断 当前节点 是否是 “根节点”；
② 如果不是，我们：
    1.先（递归地）找到 其父节点的根节点；
    2.然后使用 其父节点的根节点 来 作为它的‘新的父节点’；（路径压缩）
③ 最终（路径压缩的结果），当前节点的父节点 就会是 树的根节点。