手段：顺序遍历所有的子序列，使用 当前子序列 尝试在主序列中匹配；

步骤：
① 尝试 在主序列的当前位置上 对 当前子序列 进行匹配；
② 如果 成功匹配 ‘当前子序列’，则：
    Ⅰ 把 主序列的数字指针 移动到 完整匹配后的下一个位置；
    Ⅱ 继续尝试匹配 下一个子序列；// 关键：主序列的数字指针 一直在向后移动
③ 如果 当前位置上 ‘字符匹配’失败，则：
    Ⅰ 把 主序列的数字指针 向后移动一个位置；
    Ⅱ 继续尝试匹配 当前子序列；

概念：
① 子序列匹配；
② 数字匹配；
③ 主序列中的数字指针；
④ 子序列中的数字指针；