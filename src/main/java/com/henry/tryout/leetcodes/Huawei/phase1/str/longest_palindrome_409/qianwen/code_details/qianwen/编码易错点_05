🛡️ 编码 Checklist（针对本题）

检查项	    正确做法
数组大小	    new int[128]（覆盖 ASCII）
数据类型	    用 int[]，不用 char[]
偶数计算	    count / 2 * 2 或 count - (count & 1)
中心字符	    最多 +1，用 ans < s.length() 判断
字符范围	    直接统计 所有字符（题目保证是字母）
实现方式	    优先 数组，非必要不用 HashMap