🔑 核心解题思路：排序 + 贪心扫描

这是本题的标准解法，分为两步：
步骤 1：按 区间的起始位置 排序
① 将 intervals 按 start 升序排列
② 这样 可以保证：
    当前区间 只可能 与 前一个合并后的区间 重叠，而不会 与 更早的区间 重叠；

步骤 2：线性扫描 + 合并
① 初始化 一个结果列表 res
② 遍历 排序后的每个区间：
    Ⅰ 如果 res 为空，或 当前区间 与 res 最后一个区间 不重叠 → 直接加入
    Ⅱ 否则 → 合并：更新 res 最后一个区间的 end 为 max(原end, 当前end)