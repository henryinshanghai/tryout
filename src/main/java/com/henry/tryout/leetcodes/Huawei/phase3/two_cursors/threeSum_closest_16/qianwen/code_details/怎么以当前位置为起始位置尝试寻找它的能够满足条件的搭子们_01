手段：借助while循环 + 首尾指针 来 尝试 以其为起点/锚点的所有可能的三元组组合；

具体做法：
① 对于 当前位置currentSpot，准备一个while循环；
② 准备 两个指针：
    middleBroCursor（初始指向 当前位置的下一个位置）
    bigBroCursor（初始指向 数字序列的末尾位置）
③ 计算 当前三元组的加和结果，并 使用它：
    Ⅰ 尝试更新 ‘最接近目标和的加和值’变量；
    Ⅱ 与 targetSum 进行比较 来 按需移动指针 或 返回结果；