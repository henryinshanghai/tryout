🧠 解题思路（最优解）
步骤 1：预处理 —— 统计每行、每列的 'B' 数量
① 创建两个数组：
    Ⅰ rowCount[i] = 第 i 行中 'B' 的个数
    Ⅱ colCount[j] = 第 j 列中 'B' 的个数

步骤 2：再次遍历矩阵
① 对每个位置 (i, j)：
    Ⅰ 如果 picture[i][j] == 'B'
    Ⅱ 且 rowCount[i] == 1 且 colCount[j] == 1
    Ⅲ → 这是一个孤独像素，计数 +1

复杂度：
时间：O(m × n) —— 两次遍历
空间：O(m + n) —— 两个计数数组