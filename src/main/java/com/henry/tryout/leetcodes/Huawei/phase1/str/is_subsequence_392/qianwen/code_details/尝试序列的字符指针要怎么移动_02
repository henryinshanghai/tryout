只有 当 尝试序列的当前字符 与 文本序列的当前字符 相同（匹配）时，才 把它向后移动一个位置。

代码：
// 如果 尝试序列中的当前字符 与 文本序列中的当前字符 相同，说明 在 文本序列中 找到了一个 字符匹配，则：
if (triedCharSeq.charAt(cursorOfTriedSeq) == textCharSeq.charAt(cursorOfSourceSeq)) {
    // 把 尝试序列中的指针 向后移动一个位置
    cursorOfTriedSeq++;
}