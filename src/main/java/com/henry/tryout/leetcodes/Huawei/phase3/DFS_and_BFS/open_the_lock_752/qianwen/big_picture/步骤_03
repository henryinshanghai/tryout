🧩 解题步骤（BFS）
1.将 deadends 转为 Set<String> deadSet
2.特判：若 "0000" 在 deadSet 中，直接返回 -1
3.初始化 队列（Queue<String>），加入 "0000"
4.初始化 visited 集合，记录 已访问状态
5.BFS 循环：
    ① 按层处理（记录当前层大小）
    ② 对 每个状态，生成 8 个邻居
    ③ 若 邻居 是 target，返回 当前步数 + 1
    ④ 若 邻居 合法（不在 deadSet 且未访问），加入 队列 和 visited
6.若 BFS 结束仍未找到，返回 -1