🔍 算法核心思想图解（文字版）
假设我们有三种不同的元素 A、B、C：
① 每次遇到 三个互不相同的元素，我们可以 安全地 同时删除它们，因为：
    1.如果 某个元素 是 “多数”（> n/3），它 在剩余部分中 仍然是多数
    2.删除 不会影响 最终结果
💡 这就是 count1-- 和 count2-- 的本质：批量抵消 非多数元素
而 candidate1 和 candidate2 就是在 这个过程 中 “幸存”下来的 最可能的 多数候选。