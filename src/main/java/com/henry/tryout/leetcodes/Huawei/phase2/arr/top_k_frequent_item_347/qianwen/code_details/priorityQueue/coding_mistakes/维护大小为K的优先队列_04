手段1：先向队列中添加k个数字，再根据新数字的情况 按需维护优先队列中的数字；

具体做法:
① 先向队列中 添加k个数字；
    if(queue.size() < k) {
        queue.offer(currentNum);
    }
② 根据新数字的情况 来 维护这个 大小为k的队列；
    如果 新数字的出现频次 比起 当前队列中的最小频次 更大，
    则：出队 最小频次数字；然后 入队 当前数字；


===
手段2(推荐写法)：
不断地 向队列中追加数字，当队列中的数字 数量超过k时，直接移除其频率最小的数字；

特征：利用 最大队列的特性 来 隐式维护它；

具体做法：
① 向队列中追加数字；
    queue.offer(currentNum);
② 当队列数字数量超过k时，移除 队列中的频率最小的数字；
    if(queue.size() > k) {
        queue.poll();
    }