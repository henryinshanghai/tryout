纯数字字符串 123456789

结果字符串：
结果字符串：123-456-789

结果字符串：123-45-67

对于 纯数字字符串 中的 最后4个字符，根据题意 单独处理：
① 如果 当前字符指针 指向 倒数第3个字符 或者 倒数第二个字符，
    比如 12345678(字符指针 会指向 倒数第二个位置)，123456789（字符指针 会指向 倒数第一个位置），
说明 可以 直接把从 当前字符指针 到 纯数字字符串末尾 的所有字符 直接追加到结果字符串中；
    追加的字符串"78"，追加得到的结果：123-456-78；
    追加的字符串"789"，追加得到的结果：123-456-789；

② 如果 当前字符指针 刚好指向 倒数第4个字符，
    比如 1234567（字符指针 会指向 倒数第4个位置），我们把 4567留下来 单独处理，
说明 我们需要 把剩下的4个数组 分成两组 逐一地添加到 结果字符串中；
    Ⅰ 先追加"45"；
    Ⅱ 再追加一个"-"；
    Ⅲ 最后再追加"67"；