① 统计向 结果字符串 中 所添加的数字字符的数量 currentDigitsAmount；
② 当 currentDigitsAmount 能够被3整除时，就 向结果字符串中 添加一个'-'字符；

照此添加规则，结果字符串 所有可能的情形如下：
① 123-456-7     // 不符合题目要求
② 123-456-78    // 符合题目要求
③ 123-456-789-  // 符合题目要求，但多出了一个'-'