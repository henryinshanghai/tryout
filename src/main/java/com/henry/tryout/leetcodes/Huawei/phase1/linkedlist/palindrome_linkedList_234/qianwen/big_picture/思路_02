🧠 解题思路（最优解：快慢指针 + 反转后半段）

❌ 暴力法（O(n) 时间 + O(n) 空间）：
① 遍历 链表，存入 数组 → 双指针 判断回文
② 或用栈：前半 入栈，后半 出栈 比较
③ 但不符合 O(1) 空间要求

✅ 最优解（O(n) 时间 + O(1) 空间）：
① 用 快慢指针 找到 链表中点
② 反转 后半段 链表
③ 比较 前半段 和 反转后的后半段
④（可选）恢复链表原状
⚠️ 注意： 链表长度 可能为 奇数 或 偶数！