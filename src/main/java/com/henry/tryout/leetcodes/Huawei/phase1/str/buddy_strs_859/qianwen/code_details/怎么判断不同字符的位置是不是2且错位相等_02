① 遍历所有的字符位置；
② 当 某个位置上，源字符 与 目标字符 不相同时，
    使用一个列表 来 记录下 当前字符不相同的位置；
③ 遍历结束后：
    Ⅰ 查看 是不是 刚好存在有 两个’字符不同的位置‘：
        list.size() == 2
    Ⅱ 查看 该位置上的字符 是否错位相等：
        firstDiffCharSpot;
        secondDiffCharSpot;

        取char进行比较。