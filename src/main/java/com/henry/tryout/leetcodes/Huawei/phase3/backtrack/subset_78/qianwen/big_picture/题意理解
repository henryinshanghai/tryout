LeetCode 第 78 题：Subsets（子集） 是回溯算法中最基础、最重要的题目之一，
用于 生成一个 不含重复元素的整数数组 的所有可能子集（幂集）。

📌 题目要求
给定一个 不含重复元素的 整数数组 nums，
返回 该数组 所有可能的子集（幂集）。
解集 不能包含 重复的子集，且 可以 按任意顺序 返回。

✅ 示例：
输入: nums = [1,2,3]
输出: [[],[1],[2],[3],[1,2],[1,3],[2,3],[1,2,3]]
 你
输入: nums = [0]
输出: [[],[0]]
⚠️ 注意：本题 不包含重复元素（含重复的是第 90 题 Subsets II）。