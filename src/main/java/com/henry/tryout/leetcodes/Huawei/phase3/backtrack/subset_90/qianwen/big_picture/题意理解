LeetCode 第 90 题：Subsets II（子集 II） 是一道经典的回溯（Backtracking） 问题，
是「子集」问题的升级版，核心挑战 在于 处理 重复元素 并 避免生成 重复子集。

📌 题目要求
给定一个 可能包含重复元素的 整数数组 nums，
返回 该数组 所有可能的 子集（幂集），解集 不能包含 重复的子集。

✅ 规则：
① 子集可以按 任意顺序 返回
② 但 不能有 重复子集（如 [1,2] 和 [2,1] 视为相同，且 若有 两个 1，也 不能生成 两个相同的 [1]）

示例：
输入: nums = [1,2,2]
输出: [[],[1],[1,2],[1,2,2],[2],[2,2]]

输入: nums = [0]
输出: [[],[0]]
⚠️ 注意：输入 可能有 重复数字，但 输出子集 必须 唯一。