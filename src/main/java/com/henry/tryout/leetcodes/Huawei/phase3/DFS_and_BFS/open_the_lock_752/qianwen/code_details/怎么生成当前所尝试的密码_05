手段：获取当前密码组合，然后改变其中一个转轮的数字 来 得到新的密码；

具体做法：
① 把 当前密码字符串 转化为 字符数组；
② 获取到 当前转轮 上的字符，获取到 当前的选择（向前转/向后转）；
③ 计算 当前转轮上的新数字；// 旧字符先转数字（char - '0'），再进行运算（+10、%10）；
④ 把 计算得到的新数字 转化为 数字字符，并 赋值回字符数组；// (char)(int + '0')；
⑤ 最终，把 字符数组 转化为 字符串；// 这个字符串 就是新的密码