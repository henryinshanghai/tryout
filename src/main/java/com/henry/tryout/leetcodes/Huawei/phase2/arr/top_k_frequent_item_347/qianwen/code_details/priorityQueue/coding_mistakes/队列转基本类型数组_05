手段1：流式操作；

具体做法：
① 先得到其流对象；// .stream()
② 再 把 包装类型元素 转化为 基本类型元素；// mapToInt(i -> i)
③ 最后 转化为数组；// toArray();

===
手段2：把队列作为可迭代对象；

具体做法：
① 准备一个int[]数组；// new int[k]
② 遍历队列中的元素，并把元素逐一地添加到数组中；// for循环 + queue.poll()