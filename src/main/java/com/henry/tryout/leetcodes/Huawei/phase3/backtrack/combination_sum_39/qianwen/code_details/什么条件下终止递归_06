由于 元素可以重复选择，所以我们 需要严格控制 递归的终止，防止 递归树 无限生长下去。

手段：在 找到满足条件的路径 与 不可能再找到满足条件的路径时，及时return；

具体做法：
① 找到了 满足条件的路径：// currentTarget == 0
    1.收集路径；
    2.返回上一级调用；

② 不可能再找到 满足条件的路径(元素都是正整数)；// currentTarget < 0
    直接返回上一级调用。