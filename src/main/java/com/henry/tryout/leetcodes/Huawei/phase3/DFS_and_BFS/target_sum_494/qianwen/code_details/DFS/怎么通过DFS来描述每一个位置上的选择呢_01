原理：
    对于每一个位置上的数字，我们都面临两种选择：添加+ 或者 添加-；
    每次做选择，就相当于DFS树上产生了两个分叉。

手段：使用一个参数来描述当前位置，再使用另一个参数来描述当前选择；

具体做法：
    dfs(
        numArr,
        currentNumCursor, // 当前需要选择+/-的位置
        currentSum, // 选择后得到的加和结果
        ...
        )