定义子问题的唯一状态标识，并把子问题的计算结果 缓存起来。

具体手段：
① 定义具体的子问题：在 当前位置 上，得到 指定的加和结果；// ID(question) = current_spot + current_sum
② 在计算子问题的解之前，尝试 从缓存中 获取该子问题的解；// 使用 子问题的唯一标识（currentNumCursor + currentSum） 作为key
③ 在计算完成子问题的解后，把 子问题的ID -> 子问题的解 添加到map中；// 作为缓存