✅ 1. 问题背景：为什么需要 KMP？

暴力匹配（Brute Force）的缺陷：
① 当 模式串（pattern）与 主串（text）在某位置上 发生字符失配 时，
暴力法会：
    1.把 主串指针 回退到 当前指针位置的下一个位置；// 从该位置 重新与模式串 逐个字符地比较。
    2.把 模式指针 回退到 模式字符串的开始位置；// 从头开始匹配
② 时间复杂度最坏为 O(n × m)（n = 主串长度，m = 模式串长度）

🎯 KMP 的目标：主串指针 永不回退，只向前走，从而 达到 O(n + m) 的线性时间。