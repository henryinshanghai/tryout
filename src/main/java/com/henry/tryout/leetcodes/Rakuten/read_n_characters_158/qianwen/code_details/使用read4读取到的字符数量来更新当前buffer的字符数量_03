if (requireMoreChars()) {
    // 则：调用 read4 读取多个字符 到 缓冲区中
    // 🐖 使用 读取到的字符数量 来 更新 当前缓冲区字符数组中的字符数量
    buffersCharAmount = read4(internalCharArrBuffer);

    ...