单例Bean的setter注入方式，在对象之间存在循环依赖时，为什么会有问题？
Spring的三级缓存机制 具体 是怎么起作用解决该问题的呢？

===
一、为什么单例Bean 的 Setter注入 在 循环依赖 时 会出问题？
1. 场景设定
假设有两个 单例Bean：A 和 B。
① A 依赖于 B（A 中有 setB(B b)）。
② B 依赖于 A（B 中有 setA(A a)）。
③ 都是单例（Singleton），且 使用 Setter 注入（或字段注入 @Autowired）。

2. Spring 的默认创建流程（Without Cache）
Spring 创建 Bean 的标准流程是：
    实例化 (Instantiation) -> 属性赋值 (Populate) -> 初始化 (Initialize)。
如果不做任何 特殊处理，流程会 变成这样：
1.创建 A：
① 实例化 A：
    new A()。此时 A 是一个空壳，属性 b 为 null。
② 属性赋值 A：
    发现 A 依赖于 B。于是暂停 A的创建，转而去 创建B。
2.创建 B：
① 实例化 B：
    new B()。此时 B 是一个空壳，属性 a 为 null。
② 属性赋值 B：
    发现 B 依赖 A。于是暂停 B的创建，转而去 创建A。
3.再次创建 A：
① 实例化 A：new A()... 等等！
② 死锁发生：
    A 等着 B 创建完才能填属性。
    B 等着 A 创建完才能填属性。
    双方都在 等对方完成“属性赋值”阶段，但 谁都无法进入 该阶段，因为对方 还没创建好。
    结果：无限递归调用，最终抛出 BeanCurrentlyInCreationException 或栈溢出。

核心矛盾：
    Setter注入 要求 对象必须完全创建好（走完整个流程）才能被注入给别人。
    但在 循环依赖 中，A没好 是因为 等B，B没好 是因为 等A，形成了 死结。
注意：
    如果是构造器注入，连“实例化”这一步 都过不去（因为 new A(b) 需要 b，new B(a) 需要 a），
    所以 构造器注入的循环依赖 Spring无法解决，直接报错。

===


===


===
