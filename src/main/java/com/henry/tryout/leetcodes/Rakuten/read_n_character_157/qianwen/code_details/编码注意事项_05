💡 最佳实践建议（针对 157）

步骤与易错点：
① 从I/O中 进行一次读取，并 把读取到的字符 写入 缓冲数组中；// 读取后 需要马上判断文件是否结束
② 计算 应该向目标数组中 写入的字符数量；// min(读取到的字符数量, 目标数组当前所需要的字符数量)
③ 把 缓冲数组中的字符 写入到 目标数组中；// 两个同步移动的指针
④ 写入完成后，更新 目标数组中的字符数量；// 用于结束循环