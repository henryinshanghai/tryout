🔷 一、题目要求回顾
给定一个单链表的头节点 head，将其重新排列为：
    L₀ → L₁ → … → Lₙ₋₁ → Lₙ
    变为
    L₀ → Lₙ → L₁ → Lₙ₋₁ → L₂ → Lₙ₋₂ → …

要求：
① 原地修改（in-place），不能新建链表；
② 不允许 修改节点值，只能 调整指针。

示例：
输入：1 → 2 → 3 → 4
输出：1 → 4 → 2 → 3

输入：1 → 2 → 3 → 4 → 5
输出：1 → 5 → 2 → 4 → 3