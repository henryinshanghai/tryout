当前位置的数字选定后，需要 从 可选数字列表 中 删除 所选定的数字。

现在我们又面临着 相同的问题：从一个 按字典序排序的 排列序列 中，选择 一个目标排列；
① 按字典序排序的 排列序列 是 固定的；（相比于 原始序列 规模更小）；
② 待构造的目标排列 的 在 此子序列(子组)中的排名 = k % groupSize;

所以，步骤如下：
① 先计算出 目标排列 在 子排列序列中 的排名；
② 再计算出 子排列序列中 以首位数字 作为组别的 子组大小；
③ 得到 子问题中，目标排列 会落到的 子组组别；
④ 以此 子组组别 作为索引，从 当前可选数字列表 中 获取数字，就是 当前位的数字。