结论：在所有 所存在的元素 都被标识后，各元素所对应的位置上的元素值 都会是 负数；
映射关系：
    当前元素 -> 该元素所对应的位置/下标
    位置 -> 该位置上的元素
    位置上的元素是否是负数 <=> 位置所对应的元素 是否存在

手段：遍历每个位置，查看 该位置上的元素 是否为负数；
① 如果 为负数，说明 该位置所对应的元素 存在，则：继续遍历；
② 如果 不为负数，说明 该位置所对应的元素 不存在，则：返回 该位置所对应的元素；

元素所对应的位置 & 位置所对应的元素：
① 对于元素X，它所对应的位置是(X-1);
② 对于位置X，它所对应的元素是(X+1);

区分 位置所对应的元素 VS. 位置上的元素；