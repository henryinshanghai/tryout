题目已经保证了 每个字符串的长度 都是 相等的，因此：
我们可以：
① 先逐个位置比较 两个字符串 在该位置上的字符 是否相同；
    如果 不相同，则：记录下 该位置；
② 所有位置遍历完成后，检查：
    1.字符不相同的位置 一共有多少个？
    2.字符串1第一个位置上的字符 与 字符串2第二个位置上的字符 是否相同；
    3.字符串2第一个位置上的字符 与 字符串1第二个位置上的字符 是否相同；

🐖
① 相等的字符串 也视为 相似。
② 题目限定：给定的字符串 都是 同构异位词，因此 只需要比较 字符不同的位置 是不是 ==2 就能判断 是否相似。

综上：
if(diffCharSpotAmount > 2) return false;
...
return true;