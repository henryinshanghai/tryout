手段：向后移动 窗口的起始位置指针，直到 窗口中不再包含有‘所追加的字符’；

具体做法1（hashmap）：
① 直接找到 重复字符 先前最近一次出现的位置，
② 然后 把 窗口的起始位置指针 直接快进到 该位置的下一个位置；

具体做法2（hashset）：
① 把 起始位置指针 一步步地向后移动，直到 窗口中不再包含该重复字符；
② 然后再追加字符；

具体做法3（boolMarkArr）：
① 把 起始位置指针 一步步地向后移动（从窗口中逐步移除字符），直到 该重复字符 不再 存在于当前窗口中；
② 然后再追加字符；


起始位置的指针 应该移动到：
max(
    该重复字符 先前最近一次出现的位置 的下一个位置 // option1
    保持当前起始位置 // option2
);

Math.max(charToItsLastOccurrence.get(currentAppendingChar) + 1, windowStart)
