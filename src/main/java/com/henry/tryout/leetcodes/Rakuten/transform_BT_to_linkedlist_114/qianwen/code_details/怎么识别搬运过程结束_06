手段：currNode指针 指向 nil节点；

原理：只有 右分支上的任意一个节点 存在有 左子节点，我们就需要 把 该左子节点 搬移到右分支上；

具体做法：
while(currNodeCursor != null) {
    if(currNodeCursor.left != null) {
        /* 搬运左子树到右分支上 */
    }

    /* 更新currNodeCursor指针的位置 */
}