🔍 代码逻辑 与 字典序生成的对应关系

代码步骤	                所模拟的 字典序生成行为
available 列表	        维护 当前DFS层 可选的升序数字
index = k / groupSize	决定 跳过 前index个 完整子树，进入 目标子树
available.get(index)	选择 当前层的目标分支（即 当前位的数字）
available.remove(index)	标记 该数字已使用，下一层 不可再选
k = k % groupSize	    进入子树 后，更新为 子树内的局部序号