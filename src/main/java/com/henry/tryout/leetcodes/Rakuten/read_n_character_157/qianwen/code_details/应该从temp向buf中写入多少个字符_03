原则：向buf中 写入的字符数量 不能超过 buf当前所需要的字符数量

分类讨论：
① buf当前所需要的字符数量 > temp读取到的字符数量，
    这种情况下，把 temp读取到的字符数量 完全写入到buf中；
② buf当前所需要的字符数量 < temp读取到的字符数量，
    这种情况下，只需要 把temp中的前N个字符 写入到buf中。

总结：
    向buf中写入字符的数量 = Math.min(buf当前所需要的字符数量，temp读取到的字符数量)
