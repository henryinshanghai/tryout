错误写法：
    targetPermutation.append(currentSpotsDigit + '0');
分析：
    ① currentSpotsDigit + '0' 为 int + char，会得到 一个int的结果；
    ② SB.append(<int>) 会 向SB对象中 追加<int>的字符串表示；

正确写法：
    targetPermutation.append(currentSpotsDigit);
原理：
    SB中存在有 append(int)的方法重载；
内部实现：
    自动调用 String.valueOf(int) 转换；