使用一组指针，从前往后地 改变节点之间的连接方向。

== 一组指针：==
① prevNode指针；
② currentNode指针；

== 改变节点之间的连接方向：==
① 记录 currentNode的下一个节点；
② 把 currentNode的next 指向 prevNode节点；

③ 把整组指针 向后移动一个位置；

== 算法结束：==
当 currentNode 指向null时，说明 原始链表中的最后一个节点 也已经改变了连接方向，则：算法结束；

== 反转后的新链表的头节点：==
由prevNode指针 所指向，因此 返回prevNode指针即可。
