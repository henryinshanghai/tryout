你在 IntelliJ IDEA 的 Run 窗口中看到 带斜线的小方格（▯ 或类似符号），
这是 Unicode 空字符 \u0000（NUL）在终端中的可视化表示。

这并不是“没显示”，而是 IDE/终端 主动将 不可见字符 渲染为 占位符，以避免 你 误以为是 空格或空白。

===
✅ 为什么会出现小方格？
① char[] 数组的默认值是 \u0000（Unicode 码点 0）
② 这个字符 没有可视字形（non-printable character）
③ 现代终端（包括 IDEA 的 Run 控制台）会将其显示为：
    1.带斜线的方块 ▯
    2.空心方块 □
    3.或直接留空（但 IDEA 通常用 ▯ 表示）
🔍 这其实是 正常且安全的行为 —— 它告诉你：“这里有个不可见字符”。

===
🛠️ 如何正确查看 char[] 的内容？

==
✅ 方法 1：使用 Arrays.toString()（推荐）

    import java.util.Arrays;

    public class Main {
        public static void main(String[] args) {
            char[] arr = new char[3];
            System.out.println(Arrays.toString(arr));
            // 输出: [\u0000, \u0000, \u0000]
        }
    }
✅ 优点：明确显示 \u0000，不会 被渲染成 方块。

✅ 方法 2：转换为整数 查看 ASCII/Unicode 值
    char[] arr = new char[3];
    for (int i = 0; i < arr.length; i++) {
        System.out.println("arr[" + i + "] = " + (int)arr[i]);
        // 输出: 0, 0, 0
    }
    ✅ 优点：彻底避开 字符渲染问题，直接 看数值。

✅ 方法 3：用 String.valueOf() + 转义（谨慎使用）
    char[] arr = new char[3];
    String s = String.valueOf(arr);
    System.out.println(s); // 看起来是空的！
    System.out.println("Length: " + s.length()); // 但长度是 3！
⚠️ 不推荐 用于调试，因为你 看不到内容。

✅ 方法 4：自定义打印函数（适合频繁调试）
    public static void printCharArray(char[] arr) {
        StringBuilder sb = new StringBuilder("[");
        for (int i = 0; i < arr.length; i++) {
            if (i > 0) sb.append(", ");
            char c = arr[i];
            if (c == '\u0000') {
                sb.append("\\u0000");
            } else if (c < 32 || c >= 127) {
                sb.append("\\u").append(String.format("%04x", (int)c));
            } else {
                sb.append("'").append(c).append("'");
            }
        }
        sb.append("]");
        System.out.println(sb);
    }

    // 使用
    char[] arr = new char[] {'a', '\u0000', 'b'};
    printCharArray(arr); // 输出: ['a', \u0000, 'b']

===
⚙️ 额外：能否 让 IDEA 显示 \u0000 而不是方块？
不能直接关闭 这个行为，因为这是 终端/字体渲染层的决定，不是 IDEA 的 bug。

但你可以：
① 使用 Arrays.toString()（如上）
② 或在 Debug 模式下查看变量值（Debugger 会显示 \u0000）
💡 在 Debug 窗口里，char[] 会显示为 [\0, \0, \0] 或 \u0000，非常清晰！

===
📌 总结
问题	                | 解决方案
Run 窗口显示小方块 ▯	| 这是 \u0000 的正常可视化
想看到真实值	        | 用 Arrays.toString(arr)
想确认是不是 0	    | 用 (int)arr[i] 打印数值
调试时看不清	        | 使用 IDEA 的 Debugger 查看变量
✅ 记住：小方块 ≠ 错误，而是“这里有不可见字符”的提示！