upper-bound的目标：
从有序数组中，找到 第一个 大于target的元素 的位置。

以此为依据，把 数组中的所有元素 分为两类：
① 大于target的元素；
② 不大于target的元素；

比较 nums[middle] 与 target，
① 如果 中间位置的元素 不大于 target，说明 左半边(包括中间位置)都不可能是 目标元素，则：
    更新区间的左边界 = middle + 1;
② 如果 大于 target，说明 middle是一个候选答案，但 左边可能还有 更小的答案，则：
    更新搜索范围的右边界 = middle;