为什么 lower和upper的区别 只在于一个等号？upper又作何解释呢？

===
这是个非常深刻的问题！
“为什么 lower 和 upper 的区别只在于一个等号？”
答案的核心在于：
    它们 所定义的“边界”条件 不同，而 这个差异 恰好体现在 对“等于 target”的元素如何处理上。
我们来深入剖析：

🔁 一、重新明确两个概念（以升序数组为例）

名称	        数学定义	                            直观理解
lower-bound	最小的索引 i，使得 arr[i] ≥ target	target 可以“站进去”的最左位置
upper-bound	最小的索引 i，使得 arr[i] > target	target “站不进去”，必须 往后挪一位的位置

💡 想象你要把 target 插入数组 并 保持有序：
① lowerBound 是你 最早可以插入的位置（允许 和已有值相等）。
② upperBound 是你 最晚可以插入的位置（插在 所有相等值的后面）。

===
🧩 二、关键：如何看待“等于 target”的元素？
假设数组中 有多个 target，比如：
    arr = [1, 2, 2, 2, 3]
    target = 2
① 所有 2 的位置是 [1, 2, 3]。
② lower-bound = 1 → 第一个 ≥2 的位置（包含等于）。
③ upper-bound = 4 → 第一个 >2 的位置（跳过所有等于）。

👉 所以，“等号” 决定了是否 把 == target 的元素 视为 “满足条件”：
① lower-bound 把 == 当作“满足”
    → 遇到 == 就向左收缩（right = mid），试图找 更早的位置。
② upper-bound 把 == 当作“不满足”
    → 遇到 == 就向右走（left = mid + 1），因为要找的是 >。

===
💻 三、代码对比（只差一个等号！）
✅ lower-bound：找 ≥ target
    if (arr[mid] < target) {
        left = mid + 1;   // 不满足（太小）
    } else {
        right = mid;      // 满足（≥），可能是答案，但继续向左找
    }

✅ upper-bound：找 > target
    if (arr[mid] <= target) {  // ← 注意这里是 <=
        left = mid + 1;        // 不满足（≤），包括等于！
    } else {
        right = mid;           // 满足（>），可能是答案，继续向左找
    }

🔥 核心差异：
在 arr[mid] == target 时：
① lower-bound 走 else 分支（认为 找到了候选，尝试更左）。
② upper-bound 走 if 分支（认为 还不够大，必须往右）。
这就是“一个等号之差”背后的逻辑！

===
📏 四、实际用途：计算 target 的出现次数
有了 lower 和 upper，统计重复元素 变得 极其简单：
    int first = lowerBound(arr, target);   // 第一个 >= target
    int last  = upperBound(arr, target);   // 第一个 > target

    int count = last - first;  // 出现次数！
对上面的例子：
① first = 1, last = 4 → count = 3，正确！

===
🌐 五、类比现实：排队买票
想象一个按身高排的队（升序），你要找 身高 ≥170cm 的人：
① lower-bound：第一个身高 ≥170 的人（可能是 170、171...）。
② upper-bound：第一个身高 >170 的人（171、172...）。

如果队伍里有一群 正好 170cm 的人站在一起：
① lower-bound 指向 这群人的开头。
② upper-bound 指向 这群人的结尾 之后。

那个“等号” 就决定了 你是否 把 “正好170cm”的人 算作目标。

===
✅ 总结
对比项	        lower-bound	            upper-bound
条件	            ≥ target	            > target
处理 == target	视为满足，向左找	        视为不满足，向右走
代码差异	        if (arr[mid] < target)	if (arr[mid] <= target)
语义          	第一个满足条件的位置	    最后一个满足条件的下一个位置
应用	            插入位置、首次出现	        末次出现后一位、区间结束
所以，“一个等号”不是偶然，而是 两种边界定义的 本质区别 在代码中的直接体现。
