LeetCode 第 41 题是 「缺失的第一个正数」（First Missing Positive），
题目要求：
给你一个 未排序的整数数组 nums，请你找出 其中 没有出现的 最小的正整数。
要求 时间复杂度 为 O(n)，空间复杂度 为 O(1)。


🧠 理解题意的关键点
① 只关心 正整数（即 1, 2, 3, ...）。
② 数组 可能包含 负数、0、重复数字、很大的数。
③ 最小缺失正整数 一定 在 范围 [1, n+1] 内（n 是数组长度）。
原因：如果数组 恰好包含 1 到 n 的所有正整数，那么 答案就是 n+1；
    否则，一定在 1～n 中缺了一个。