三、灵魂拷问：为什么要三级缓存？二级不够吗？
这是面试中最难的部分。
既然 二级缓存 就能存“早期对象”，为什么还要 搞个 三级缓存 存“工厂”？
答案：为了保证 AOP代理对象的一致性。

场景假设：只有二级缓存
如果只有二级缓存，我们 在“实例化 A”之后，必须立刻决定：
    是把 原始对象A 放入二级缓存中，还是 把 代理对象Proxy-A 放入二级缓存中？
1.如果我们放 原始对象A：
    ① B 拿到了 原始A。
    ② 后来 A初始化时，发现 需要AOP，生成了 Proxy-A。
    ③ 问题：容器中现在有两个 A！
        B 持有的是 原始A，
        其他人拿到的是 一级缓存里的 Proxy-A。破坏了 单例模式！
2.如果我们放 代理对象Proxy-A：
    ① 在 实例化A 之后，立刻创建代理。
    ② 问题：
        万一 A 最终不需要 AOP 呢？（比如配置错了，或者条件注解不满足）。
        那我们 提前创建代理 就是 浪费性能。
        而且，代理的创建 通常依赖于 Bean的一些元数据，可能在 实例化阶段 还没完全准备好。

===
三级缓存的精妙之处：延迟决策 (Lazy Evaluation)
三级缓存 存的是 ObjectFactory。
① 它 不立即创建 代理对象。
② 只有 当 真的发生 循环依赖（B 来找 A）时，才会 调用工厂。
③ 工厂内部逻辑：
    // 伪代码
    if (需要AOP) {
        return 创建代理对象(); // 此时才创建代理
    } else {
        return 原始对象;
    }
④ 这样既保证了：
    如果 不需要AOP，就 不创建代理（节省资源）。
    如果 需要AOP 且 发生循环依赖，B 拿到的 肯定是 代理对象。
    等到 A自己初始化 时，发现 二级缓存里 已经是 代理对象了，就 直接用那个，
    保证 B拿到的 和 最终容器里的 是同一个对象。