就leetcode实际测试来看：
① 如果 先把指针归零，再判断文件是否结束，则：AC
② 如果 先判断文件是否结束，再把指针归零，则：无法通过测试用例

输入
file =
"abc"
queries =
[4,1]

输出
["abc","\u0000"]
预期结果
["abc",""]

===
\u0000 是 char[]数组元素的默认值

✅ 总结
    \u0000 = Unicode 空字符 = Java char 的默认值 = ASCII 0 = NUL
