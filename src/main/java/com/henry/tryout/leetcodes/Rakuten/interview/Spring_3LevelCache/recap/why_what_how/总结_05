四、总结
1.Setter 循环依赖问题：源于“必须 完全创建好 才能注入”的死锁逻辑。
2.解决思路：提前暴露 “半成品”引用，打破 等待链。
3.三级缓存作用：
    ① 一级：成品池。
    ② 二级：半成品池（解决循环依赖的临时中转站）。
    ③ 三级：工厂池（核心！用于 延迟创建 代理对象，确保 单例一致性）。
4.局限性：
    ① 只能解决 单例 (Singleton) 的 Setter/字段注入 循环依赖。
    ② 构造器注入 循环依赖 无法解决（因为 实例化 都过不去，没法 进缓存）。
    ③ 原型 (Prototype) Bean 无法解决（因为原型不缓存）。

这就是 Spring 三级缓存机制解决循环依赖的全貌，既巧妙又严谨！