二、Spring 三级缓存机制 是如何解决的？
Spring的解决方案 核心思想 是：提前暴露。
即：
    虽然 A 还没完成 “属性赋值”和“初始化”，
    但我先把 A的原始引用（或者 代理对象的工厂）拿出来 给B用。
    只要 B 能拿到 A的引用 填进去，B 就能 创建完成；
    B 完成后，A 也能拿到 B的引用，从而 解开死锁。

为了实现这个 “提前暴露” 并 兼顾 AOP代理，Spring 设计了 三级缓存。

1. 三级缓存的定义
Spring 在 DefaultSingletonBeanRegistry 中定义了三个 Map：
① 一级缓存 (singletonObjects)：
    用于存放 已经完全初始化好的 Bean（成品）。
    状态：实例化 + 属性赋值 + 初始化 + AOP 代理完成。
② 二级缓存 (earlySingletonObjects)：
    用于存放 原始的Bean实例（半成品），或者 早期的代理对象。
    状态：已实例化，但未属性赋值，未初始化。
    作用：解决 循环依赖，让 其他Bean 能提前拿到 引用。
③ 三级缓存 (singletonFactories)：
    用于存放ObjectFactory（一个函数式接口，本质是个 Lambda表达式/工厂方法）。
    作用：延迟创建 代理对象。
    只有当 确实需要 代理（AOP）且 发生循环依赖 时，才调用 这个工厂
    生成代理对象 放入 二级缓存。

2. 解决 循环依赖的详细流程（图解版）
让我们重新走一遍 A 和 B 的循环依赖，看看 三级缓存 怎么介入：
步骤 1：创建 A
1.实例化 A：
    new A()。
2.提前暴露：
    Spring 发现 A是单例 且 可能循环依赖，于是将 A的工厂对象 放入 三级缓存。
    ① singletonFactories.put("A",
        () -> getEarlyBeanReference("A", beanDefinition, A))
    ② 注意：此时还没生成 代理对象，只是 存了个 “能生成代理的工厂”。
3.属性赋值 A：
    发现依赖 B。去容器找 B。
    ① 一级、二级、三级都没 B。
    ② 暂停 A，转去创建 B。

步骤 2：创建 B
1.实例化 B：new B()。
2.提前暴露：将 B的工厂 放入三级缓存。
3.属性赋值 B：
    发现依赖 A。去容器找 A。
    一级缓存？无。
    二级缓存？无。
    三级缓存？ 有！ 找到了 A的工厂。
4.获取 早期A的引用：
    ① 调用 三级缓存中的工厂 getObject()。
    ② 关键点：工厂内部 判断 A 是否需要 AOP 代理。
        如果 不需要 AOP：直接返回 原始的 A对象。
        如果 需要 AOP：立即创建 A的代理对象 (Proxy A)。
    ③ 将 得到的对象（原始A 或 代理A）放入 二级缓存 (earlySingletonObjects)。
    ④ 从 三级缓存 中 移除 A的工厂。
    ⑤ 将 这个早期引用 注入给 B (b.setA(earlyA))。
5.继续 B 的后续流程：
    ① B 的属性赋值完成。
    ② B 执行初始化（Aware, PostProcessor, init-method）。
    ③ B 创建完成！放入一级缓存。

步骤 3：回到 A
1.恢复 A的属性赋值：
    ① 此时 B 已经创建好 并 在一级缓存中了。
    ② Spring 从一级缓存中 拿到B，注入给 A (a.setB(b))。
2.继续 A 的后续流程：
    ① A 执行初始化。
    ② 关键检查：如果 A 需要 AOP 代理。
        Spring 会检查 二级缓存中 是否已经有 A的代理对象 了？
        情况一（B 触发过代理）：
            如果在步骤2 中，因为 B 依赖于 A 且 A需要AOP，工厂 已经生成了 代理对象 放入了二级缓存。
            那么 A 在初始化时，会直接使用 二级缓存中的代理对象，保证 单例一致性。
        情况二（无循环依赖 或 无需代理）：
            正常创建 代理，替换掉 原始对象。
    ③ A 创建完成！放入一级缓存。
    ④ 清理二级缓存。