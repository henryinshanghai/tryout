参与轮转操作的四个位置的坐标：
    左上角坐标：(i, j)
    左下角：(length - 1 - j, i)
    右下角：(length - 1 - i, length - 1 - j)
    右上角：(j, length - 1 - i)

轮转的步骤：
① 使用 temp 来 记录 左上角位置的元素；
② 把 左下角的元素 搬移到 左上角位置；
③ 把 右下角的元素 搬移到 左下角位置；
④ 把 右上角的元素 搬移到 右下角位置；
⑤ 把 temp元素 搬移到 右上角位置。