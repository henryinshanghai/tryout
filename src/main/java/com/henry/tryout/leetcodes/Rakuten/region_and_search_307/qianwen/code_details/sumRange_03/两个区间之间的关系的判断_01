数轴上的两个区间：[a, b] 与 [c, d]

所有可能的关系(互斥)：
① 完全没有交集；
    a > d || b < c
② 一个区间 被 另一个区间覆盖；
    a <= c && d <= b
③ 区间之间 部分重叠；
    all others.

实现互斥的手段：
    if(condition1) {
        return A;
    }

    if(condition2) {
        return B;
    }

    // condition3
    return C;

===
✅ 总结
问题	                | 答案
为什么不用 else if？	| 因为前两个 if 都 return 了，控制流 天然互斥
这样写安全吗？	    | ✅ 安全！这是递归函数的标准写法
是否符合最佳实践？	    | ✅ 符合 “卫语句”原则：坏情况/终止条件 优先处理并返回
能否改成 else if？	| 可以，但没必要，反而增加嵌套
核心思想是什么？	    | 用 return 实现 隐式的互斥分支


💡 记住这个模式：
在递归或深度逻辑中，用 if (condition) return ...; 处理边界/终止条件，
剩下的就是主逻辑 ———— 无需 else！