🔑 二、你需要掌握的核心概念
概念	                    | 为什么重要
1. 缓冲区（Buffer）管理	| read4 一次读 4 个，但用户 可能 只取 1 个，剩下的 3 个 要暂存
2. 跨函数调用的 状态保持	| 必须用 类成员变量 保存 未消费的字符 和 位置
3. 指针/索引操作	        | 精确控制 从哪里读、写到哪里
4. 边界条件处理	        | 文件结束、n=0、缓冲区刚好用完 等

💡 核心思想：用一个 内部缓冲区（internal buffer）暂存 read4 多读的数据，并 在 后续调用 中 消费它。

