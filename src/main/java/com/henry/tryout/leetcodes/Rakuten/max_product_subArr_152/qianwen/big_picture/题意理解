📋 题目描述
给定一个 整数数组nums，找出数组中 乘积最大的 非空连续子数组，返回 该子数组的乘积。
示例：
输入：[2,3,-2,4]
    → 输出：6（子数组 [2,3]）
输入：[-2,0,-1]
    → 输出：0


🔍 核心难点
这道题 与 "最大子数组和"（LeetCode 53）类似，但有一个关键区别：

问题	    最大子数组和	    乘积最大子数组
负数影响	负数只会让和变小	负数×负数=正数，可能变成最大值
需要维护	只需最大值	    最大值和最小值都要维护

关键洞察： 一个很小的负数（最小值）乘以 另一个负数，可能会 变成 很大的正数（最大值）！