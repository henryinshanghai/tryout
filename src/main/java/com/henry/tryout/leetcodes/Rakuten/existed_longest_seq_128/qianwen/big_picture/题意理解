题目要求：
给定一个 未排序的整数数组 nums，找出其中 最长连续序列的长度。
要求时间复杂度为 O(n)。

📌 一、题目示例
输入: nums = [100, 4, 200, 1, 3, 2]
输出: 4
解释: 最长连续序列为 [1, 2, 3, 4]，长度为 4。

注意：
① 序列 不要求 在原数组中连续，只要 数值连续 即可
② 不能排序（否则 O(n log n)，不符合 O(n) 要求）