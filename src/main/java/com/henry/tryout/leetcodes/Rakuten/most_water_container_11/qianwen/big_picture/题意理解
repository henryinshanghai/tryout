给定一个长度为 n 的非负整数数组 height，其中每个元素 height[i] 表示在坐标 i 处有一条高度为 height[i] 的竖直线。
选择两条线，使得它们与 x 轴构成的容器可以容纳最多的水。

容器的面积 = 两条线之间的距离 × 较短那条线的高度
即：area = (j - i) * min(height[i], height[j])

✅ 示例：
输入: height = [1,8,6,2,5,4,8,3,7]
输出: 49
解释: 选择 i=1 (h=8) 和 j=8 (h=7)，宽度=7，高度=min(8,7)=7 → 面积=49