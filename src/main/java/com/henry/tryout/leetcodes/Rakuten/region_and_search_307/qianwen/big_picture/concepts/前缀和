前缀和（Prefix Sum） 是一种非常基础且强大的算法技巧，
用于 快速计算 数组中 任意连续子数组 的和。

===
🌟 一句话定义
前缀和数组 prefix 中的第i项，表示 原数组 nums 从第0项 到 第i-1项 的累加和。

更形式化地说：
① 原数组：nums[0], nums[1], ..., nums[n-1]
② 前缀和数组：
    prefix[0] = 0
    prefix[1] = nums[0]
    prefix[2] = nums[0] + nums[1]
    ...
    prefix[i] = nums[0] + nums[1] + ... + nums[i-1]
即：
    prefix[i] = Σ_{k=0}^{i-1} nums[k]

===
🧮 举个例子
原数组 nums = [2, 3, 1, 4]

构建前缀和数组 prefix（长度为 n+1 = 5）：

i	prefix[i]	含义
0	0	空和
1	2	nums[0]
2	2+3 = 5	nums[0..1]
3	2+3+1 = 6	nums[0..2]
4	2+3+1+4 = 10	nums[0..3]

所以：prefix = [0, 2, 5, 6, 10]

===
🔑 核心用途：O(1) 计算任意子数组和
要计算 nums[i] + nums[i+1] + ... + nums[j]（即 sum(i, j)）：

✅ 公式：
    sum(i, j) = prefix[j + 1] - prefix[i]
为什么？
    ① prefix[j+1] = nums[0] + ... + nums[j]
    ② prefix[i] = nums[0] + ... + nums[i-1]
    ③ 相减后，剩下 nums[i] + ... + nums[j]

例子：
求 nums[1..3] = 3 + 1 + 4 = 8
    → prefix[4] - prefix[1] = 10 - 2 = 8 ✅

===
⚙️ 如何构建 前缀和数组？
    int[] nums = {2, 3, 1, 4};
    int n = nums.length;
    int[] prefix = new int[n + 1]; // 长度 n+1

    for (int i = 0; i < n; i++) {
        prefix[i + 1] = prefix[i] + nums[i];
    }
    // 结果: prefix = [0, 2, 5, 6, 10]
① 时间复杂度：O(n)（预处理一次）
② 空间复杂度：O(n)

===
✅ 优势 vs 暴力法
方法	    | 预处理时间	| 单次查询时间 | 适用场景
暴力法	| 无	    | O(n)	     | 查询很少
前缀和	| O(n)	    | O(1)	     | 多次查询
💡 如果你要回答 1000 次“某段和是多少”，前缀和能从 O(1000×n) 降到 O(n + 1000)！

===
📌 经典应用场景
① LeetCode 303. Range Sum Query - Immutable
    → 数组不变，多次查询区间和 → 直接用前缀和

② 子数组和为 K 的个数（LeetCode 560）
    → 利用“前缀和 + 哈希表”优化到 O(n)

③ 和为 K 的最短/最长子数组
    → 结合滑动窗口或哈希表

④ 二维前缀和（矩阵区域和）
    → 扩展到图像处理、棋盘问题等

===
⚠️ 注意事项
① 前缀和 只适用于 “不可变数组”
    → 如果数组 会频繁更新（如 LeetCode 307），前缀和 每次更新要 O(n)，不高效！
    → 此时需用 线段树 或 树状数组

② 下标容易出错
    → 强烈建议使用 prefix[0] = 0 的 n+1 长度版本，避免 边界判断

===
✅ 总结
概念	        | 说明
前缀和是什么	| 原数组从开头到当前位置的累加和
核心公式	    | sum(i, j) = prefix[j+1] - prefix[i]
最大优点	    | O(1)时间内 回答 区间和查询
前提条件	    | 数组内容不会改变（静态）
典型题号	    | LeetCode 303、560、523、974 等
💡 记住：只要看到 “多次查询 子数组和”，先想 前缀和！