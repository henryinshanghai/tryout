表格
角度	    解释
定义层面	单个元素 是 长度为 1 的合法递增/递减序列
逻辑层面	“无法接上前一段”时，应 重新开始，长度为 1
递推层面	dp[i] = dp[i-1] + 1 要求 dp[i-1] 至少为 1
错误后果	初始化为 0 会导致 长度整体偏小，甚至漏掉 有效山脉
通用原则	所有“子数组长度”类 DP，初始值通常为 1

💡 记住一句话：
“任何元素自己，就是一个长度为 1 的山脚。”