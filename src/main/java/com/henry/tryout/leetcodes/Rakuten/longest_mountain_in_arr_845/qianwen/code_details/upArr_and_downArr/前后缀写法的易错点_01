✅ 总结：易错点清单
表格
易错点	            正确做法
①.up/down定义模糊	up[i] 以 i 结尾递增，down[i] 以 i 开头递减
② 重置为 0	        永远重置为 1
③ 用 >= 或 <=	    必须用 >（严格）
④ 忘记检查 峰顶有效性	加 if (up[i] > 1 && down[i] > 1)
⑤ 返回长度 < 3	    逻辑上不会发生，但心里要有数
⑥ 使用三目运算符时 >3 应该是 >=3

只要你避开这 6 个坑，前缀+后缀方法就是面试中最稳、最清晰的选择！