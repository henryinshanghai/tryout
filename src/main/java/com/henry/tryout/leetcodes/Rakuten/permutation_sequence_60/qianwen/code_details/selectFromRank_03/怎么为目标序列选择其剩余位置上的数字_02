🐖 在 当前位置的数字 选定后，需要 从 可选数字列表 中 删除 所选定的数字。

现在我们 又面临着 相同的问题：
从一个 ‘按字典序排序的 排列序列’ 中，选择 一个‘目标排列’；
已知：
① 按字典序排序的 排列序列 是 固定的；（相比于 原始序列 规模更小）；
② ‘待构造的目标排列’ 在 此子序列(子组)中的排名 = k % groupSize;

具体做法：
① 先计算出 目标排列 在 子排列序列中 的排名；
② 再计算出 子排列序列中 以 首位数字 作为组别的 子组大小；
③ 得到 子问题中，目标排列 会落到的 具体的子组组别；
④ 以 此子组组别 作为索引，从 当前可选数字列表 中 获取数字，就是 当前位的数字。