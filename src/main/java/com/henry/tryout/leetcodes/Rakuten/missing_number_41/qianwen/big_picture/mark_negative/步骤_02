解法一：负数标记法（推荐初学者理解）

步骤：
① 预处理：将所有 ≤0 或 >n 的数 改为 n+1（它们 不影响 答案）
② 标记存在：
    遍历数组，对每个 x = |nums[i]|，
    若 x ≤ n，则令 nums[x-1] = -|nums[x-1]|
③ 找答案：
    第一个 nums[i] > 0 的位置 → 缺失 i+1；
    若全负，返回 n+1

所需理解：
① 为什么 能 用符号 做标记？→ 因为我们 只关心 “是否出现”，不关心 原始值大小（只要它是正的）
② 为什么要 取绝对值？→ 防止 已被标记的负数 被误读