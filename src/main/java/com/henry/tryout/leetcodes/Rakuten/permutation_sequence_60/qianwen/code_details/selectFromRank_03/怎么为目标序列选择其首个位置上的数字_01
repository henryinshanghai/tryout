手段：先计算出 当前位上的数字 在可选数字列表中的索引，再 选择它；

具体做法（数学计算）：
① 计算出 当前位上的数字 在列表中的索引：
    int itsIndex = (k-1) / subGroupSize;
② 获取到 当前位上的数字：
    availableDigitSeqInAsc.get(itsIndex);

🐖：数字被选择后，需要 把 它 从 可选数字列表 中（保持有序性地）移除。