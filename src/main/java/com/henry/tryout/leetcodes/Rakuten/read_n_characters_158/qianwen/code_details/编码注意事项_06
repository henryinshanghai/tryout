步骤与易错点：
① 在多次调用之间 保持‘状态’;
    1.没有读完的缓冲数组；
    2.待处理的字符指针位置；
    3.缓冲数组中的字符数量；
② 根据需要，从I/O中 读取一次字符，并 把读取的结果写入到 内部缓冲数组中；
     1.只有在当前缓冲数组中的字符用尽后，才需要再次读取；
     2.读取字符后，按需更新 成员变量所表示的状态；
     3.读取字符后，需要随即检查 文件是否结束；
③ 把 当前缓冲数组中的当前字符 写入到 目标数组中的正确位置；
     1.一次循环只写入一个字符；
     2.写入字符后，更新字符指针、位置指针；
