Kotlin: Module was compiled with an incompatible version of Kotlin.
The binary version of its metadata is 1.7.1, expected version is 1.5.1.

IDEA中的kotlin插件（版本为1.5）
查的文章说是升级 kotlin插件的版本，结果最新版本就是1.5 没得升级。
这里把 spring-boot的版本回退到 2.4.3. 推测可能是它所包含的新依赖 要求使用Kotlin的更高版本

#2 java: 程序包com.github.dreamhead.moco不存在
这个moco的程序库好像没有在 maven仓库中，所以只能通过外部依赖的方式 来 引入。
参考：IDEA中引入所依赖的外部jar包。

终于运行成功了 😓